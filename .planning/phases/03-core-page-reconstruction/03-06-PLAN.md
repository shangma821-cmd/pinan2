---
phase: 03-core-page-reconstruction
plan: 06
type: execute
wave: 3
depends_on:
  - "03-02"
  - "03-03"
  - "03-04"
  - "03-05"
files_modified:
  - tests/landing-core-pages.spec.ts
  - tests/landing-routing.spec.ts
autonomous: false
requirements:
  - HOME-01
  - HOME-02
  - ABOUT-01
  - ABOUT-02
  - PROD-01
  - PROD-02
  - PROD-03
  - FRAN-01
  - FRAN-02
  - FRAN-03
user_setup: []
must_haves:
  truths:
    - Automated browser coverage exists for every Phase 3 requirement ID while the existing route smoke remains green.
    - The shared shell and News route continue working after the core-page reconstruction lands.
    - Desktop/mobile parity still needs one explicit human spot-check even after browser automation passes.
  artifacts:
    - tests/landing-core-pages.spec.ts
    - tests/landing-routing.spec.ts
  key_links:
    - `tests/landing-core-pages.spec.ts` contains the exact test titles `home page reconstruction`, `home CTA routes`, `about page reconstruction`, `products dual view`, and `franchise page reconstruction`.
    - `tests/landing-routing.spec.ts` keeps the existing titles `entry-station shell ownership` and `five landing routes`.
    - Full verification uses `npm run build && npx playwright test tests/landing-routing.spec.ts tests/landing-core-pages.spec.ts`.
---

<objective>
Harden verification for the reconstructed core pages and confirm route-family behavior remains intact.

Purpose: Turn Phase 3 into an executable, test-backed delivery pack so execution can prove page parity anchors and route health instead of relying on visual claims alone.
Output: A new core-page Playwright spec, strengthened route smoke checks, and a final human parity checkpoint.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
@$HOME/.codex/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-core-page-reconstruction/03-CONTEXT.md
@.planning/phases/03-core-page-reconstruction/03-RESEARCH.md
@.planning/phases/03-core-page-reconstruction/03-VALIDATION.md
@tests/landing-routing.spec.ts
@landing/pages/LandingHomePage.tsx
@landing/pages/LandingAboutPage.tsx
@landing/pages/LandingProductsPage.tsx
@landing/pages/LandingFranchisePage.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a dedicated Phase 3 Playwright spec for Home, About, Products, and Franchise</name>
  <files>tests/landing-core-pages.spec.ts</files>
  <read_first>.planning/phases/03-core-page-reconstruction/03-RESEARCH.md, .planning/phases/03-core-page-reconstruction/03-VALIDATION.md, tests/landing-routing.spec.ts, landing/pages/LandingHomePage.tsx, landing/pages/LandingAboutPage.tsx, landing/pages/LandingProductsPage.tsx, landing/pages/LandingFranchisePage.tsx</read_first>
  <action>Create `tests/landing-core-pages.spec.ts` with these exact test titles: `home page reconstruction`, `home CTA routes`, `about page reconstruction`, `products dual view`, and `franchise page reconstruction`. `home page reconstruction` must `page.goto('/entry-station')` and assert visible anchors `AI细胞修复`, `告别亚健康`, `传统模式3大痛点`, `4大核心壁垒，同行难复制`, `服务闭环，无断点体验`, `成果与声誉`, and `资讯动态`. `home CTA routes` must click `了解产品服务` and `加盟合作` and assert the URLs `/entry-station/products` and `/entry-station/franchise`. `about page reconstruction` must assert `权威资质认证`, `发展历程`, `专业团队与设备`, `贴心服务体验`, `2018`, and `2024`. `products dual view` must assert the default `核心产品` view, click `会员套餐`, and then assert `周卡`, `年卡`, and `推荐`. `franchise page reconstruction` must assert `合作方案`, `店中店收益测算（示例）`, `全程扶上马，帮你赚到钱`, `3大承诺，零风险加盟`, `立即申请加盟`, and the contact anchor `18948301116`.</action>
  <automated>test -f tests/landing-core-pages.spec.ts && rg -n 'home page reconstruction|home CTA routes|about page reconstruction|products dual view|franchise page reconstruction' tests/landing-core-pages.spec.ts && npm run build && npx playwright test tests/landing-core-pages.spec.ts</automated>
  <verify>test -f tests/landing-core-pages.spec.ts && rg -n 'AI细胞修复|传统模式3大痛点|4大核心壁垒，同行难复制|服务闭环，无断点体验|成果与声誉|资讯动态' tests/landing-core-pages.spec.ts && rg -n '/entry-station/products|/entry-station/franchise' tests/landing-core-pages.spec.ts && rg -n '权威资质认证|发展历程|专业团队与设备|贴心服务体验|2018|2024' tests/landing-core-pages.spec.ts && rg -n '周卡|年卡|推荐' tests/landing-core-pages.spec.ts && rg -n '合作方案|店中店收益测算（示例）|全程扶上马，帮你赚到钱|3大承诺，零风险加盟|立即申请加盟|18948301116' tests/landing-core-pages.spec.ts && npm run build && npx playwright test tests/landing-core-pages.spec.ts</verify>
  <acceptance_criteria>
    - `tests/landing-core-pages.spec.ts` exists
    - `tests/landing-core-pages.spec.ts` contains `home page reconstruction`
    - `tests/landing-core-pages.spec.ts` contains `home CTA routes`
    - `tests/landing-core-pages.spec.ts` contains `about page reconstruction`
    - `tests/landing-core-pages.spec.ts` contains `products dual view`
    - `tests/landing-core-pages.spec.ts` contains `franchise page reconstruction`
    - `npx playwright test tests/landing-core-pages.spec.ts` exits 0
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Phase 3 has browser assertions that directly cover the restored core-page requirements.</done>
</task>

<task type="auto">
  <name>Task 2: Strengthen the existing route smoke without changing its test titles</name>
  <files>tests/landing-routing.spec.ts</files>
  <read_first>.planning/phases/03-core-page-reconstruction/03-RESEARCH.md, .planning/phases/03-core-page-reconstruction/03-VALIDATION.md, tests/landing-routing.spec.ts, landing/components/LandingShell.tsx, landing/components/LandingNav.tsx, landing/components/LandingFooter.tsx, landing/pages/LandingNewsPage.tsx</read_first>
  <action>Update `tests/landing-routing.spec.ts` but keep the exact titles `entry-station shell ownership` and `five landing routes`. Preserve the current route list and direct-load checks. Add assertions that shared shell markers remain visible after the Phase 3 rebuild and that the shared nav CTA `了解加盟政策` appears on React-owned routes. Keep the `/entry-station/news` route in the smoke list so News remains reachable even though full News parity is deferred to Phase 4. Do not rename existing tests or remove any route from the smoke loop.</action>
  <automated>rg -n 'entry-station shell ownership|five landing routes' tests/landing-routing.spec.ts && rg -n '了解加盟政策' tests/landing-routing.spec.ts && npm run build && npx playwright test tests/landing-routing.spec.ts</automated>
  <verify>rg -n 'entry-station shell ownership|five landing routes' tests/landing-routing.spec.ts && rg -n '/entry-station/news' tests/landing-routing.spec.ts && rg -n '了解加盟政策' tests/landing-routing.spec.ts && npm run build && npx playwright test tests/landing-routing.spec.ts</verify>
  <acceptance_criteria>
    - `tests/landing-routing.spec.ts` contains `entry-station shell ownership`
    - `tests/landing-routing.spec.ts` contains `five landing routes`
    - `tests/landing-routing.spec.ts` contains `/entry-station/news`
    - `tests/landing-routing.spec.ts` contains `了解加盟政策`
    - `npx playwright test tests/landing-routing.spec.ts` exits 0
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>The original route smoke stays intact while also validating the upgraded shared shell.</done>
</task>

<task type="checkpoint:human-verify" gate="non-blocking">
  <name>Task 3: Spot-check core-page parity on desktop and mobile</name>
  <files>landing/pages/LandingHomePage.tsx, landing/pages/LandingAboutPage.tsx, landing/pages/LandingProductsPage.tsx, landing/pages/LandingFranchisePage.tsx</files>
  <action>After the automated suites pass, do one manual visual pass on `/entry-station`, `/entry-station/about`, `/entry-station/products`, and `/entry-station/franchise` in one desktop viewport and one narrow mobile viewport. Confirm the shared shell still frames every page, the Home hero and photography feel recognizably baseline-led, the Products toggle remains understandable, and the Franchise form/contact block is legible without backend wiring. If no human reviewer is available, record the follow-up explicitly and continue because this checkpoint is non-blocking for plan completeness.</action>
  <what-built>Automated browser coverage for core pages plus preserved route smoke for the landing route family.</what-built>
  <how-to-verify>Open the four Phase 3 routes in desktop and narrow mobile widths. Confirm baseline section density is preserved and no page collapses into generic placeholder cards.</how-to-verify>
  <resume-signal>Type "approved" or note the remaining visual follow-up</resume-signal>
  <automated>npm run build && npx playwright test tests/landing-routing.spec.ts tests/landing-core-pages.spec.ts</automated>
  <verify>Human approved the visual spot-check, or the summary explicitly records the manual follow-up as pending.</verify>
  <done>The phase has both automated proof and a clear final manual parity pass.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `test -f tests/landing-core-pages.spec.ts && rg -n 'home page reconstruction|home CTA routes|about page reconstruction|products dual view|franchise page reconstruction' tests/landing-core-pages.spec.ts`
- [ ] `rg -n 'entry-station shell ownership|five landing routes' tests/landing-routing.spec.ts && rg -n '了解加盟政策' tests/landing-routing.spec.ts`
- [ ] `npm run build`
- [ ] `npx playwright test tests/landing-routing.spec.ts tests/landing-core-pages.spec.ts`
</verification>

<success_criteria>

- All tasks completed
- Browser tests cover every Phase 3 requirement ID
- Existing route smoke still protects shared shell and News route reachability
- Manual desktop/mobile parity check is either approved or explicitly recorded as pending follow-up
  </success_criteria>

<output>
After completion, create `.planning/phases/03-core-page-reconstruction/03-06-SUMMARY.md`
</output>
