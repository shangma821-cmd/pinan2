---
phase: 05-react-runtime-cutover-parity-qa
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - EntryShell.tsx
  - landing/assets.ts
autonomous: true
requirements:
  - XPG-03
must_haves:
  truths:
    - `/` shell iframe points to `/entry-station`, not `/entry-station/index.html`.
    - Landing assets are resolved through React/Vite build ownership rather than public-path string literals.
  artifacts:
    - EntryShell.tsx
    - landing/assets.ts
  key_links:
    - `EntryShell.tsx` exposes `data-testid="entry-shell-landing-frame"` with `src="/entry-station"`.
    - `landing/assets.ts` imports landing media from the repository and exports bundler-owned asset URLs.
---

<objective>
Switch shell/runtime ownership to the React landing and move landing media onto the Vite asset pipeline.
</objective>
