---
phase: 02-react-landing-foundation-post-reset
plan: 02
type: execute
wave: 1
depends_on:
  - "02-01"
files_modified:
  - EntryShell.tsx
  - landing/LandingApp.tsx
  - landing/routes.tsx
  - landing/routeMetadata.ts
autonomous: true
requirements:
  - SHELL-02
user_setup: []
must_haves:
  truths:
    - `/` must remain iframe-home mode and continue to point at `/entry-station/index.html` during Phase 2.
    - `/academy` must remain the existing academy experience with the current shell bridge behavior intact.
    - `/entry-station` and all scoped subroutes must become React-owned shell territory rather than falling back into iframe-home mode.
  artifacts:
    - EntryShell.tsx
    - landing/LandingApp.tsx
    - landing/routes.tsx
    - landing/routeMetadata.ts
  key_links:
    - `EntryShell.tsx` introduces an explicit landing mode for `/entry-station*` while preserving the current iframe target and academy mode.
    - `landing/routeMetadata.ts` is the single source of truth for the five Phase 2 routes and nav labels.
    - `landing/routes.tsx` mounts the shared landing route tree under `basename="/entry-station"`.
---

<objective>
Teach the host shell to hand off `/entry-station*` to a dedicated React landing router.

Purpose: Make the Phase 2 route family reachable under the real public route namespace without disturbing `/` iframe behavior or `/academy`.
Output: A tri-mode `EntryShell`, a dedicated landing app entry, and a centralized route table for the five landing destinations.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-react-landing-foundation-post-reset/02-CONTEXT.md
@.planning/phases/02-react-landing-foundation-post-reset/02-RESEARCH.md
@.planning/phases/02-react-landing-foundation-post-reset/02-VALIDATION.md
@.planning/phases/01-baseline-lock-shell-contract/01-UI-SPEC.md
@EntryShell.tsx
@index.tsx
@public/entry-station/index.html
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a centralized landing route contract</name>
  <files>landing/routeMetadata.ts</files>
  <read_first>.planning/phases/02-react-landing-foundation-post-reset/02-CONTEXT.md, .planning/phases/02-react-landing-foundation-post-reset/02-RESEARCH.md, .planning/phases/01-baseline-lock-shell-contract/01-UI-SPEC.md, landing/routeMetadata.ts</read_first>
  <action>Create `landing/routeMetadata.ts` exporting one array of five route records with these exact path/name pairs: `('/', '首页')`, `('/about', '关于我们')`, `('/products', '产品服务')`, `('/franchise', '加盟合作')`, and `('/news', '新闻动态')`. Each record must also include a stable page key using the exact identifiers `home`, `about`, `products`, `franchise`, and `news`, because later plans will use these keys for page markers, nav active state, and route smoke assertions.</action>
  <automated>test -f landing/routeMetadata.ts && rg -n "'/'" landing/routeMetadata.ts && rg -n "'/about'" landing/routeMetadata.ts && rg -n "'/products'" landing/routeMetadata.ts && rg -n "'/franchise'" landing/routeMetadata.ts && rg -n "'/news'" landing/routeMetadata.ts && rg -n "首页|关于我们|产品服务|加盟合作|新闻动态" landing/routeMetadata.ts</automated>
  <verify>test -f landing/routeMetadata.ts && rg -n "home" landing/routeMetadata.ts && rg -n "about" landing/routeMetadata.ts && rg -n "products" landing/routeMetadata.ts && rg -n "franchise" landing/routeMetadata.ts && rg -n "news" landing/routeMetadata.ts</verify>
  <acceptance_criteria>
    - `landing/routeMetadata.ts` exists
    - `landing/routeMetadata.ts` contains `'/'`
    - `landing/routeMetadata.ts` contains `'/about'`
    - `landing/routeMetadata.ts` contains `'/products'`
    - `landing/routeMetadata.ts` contains `'/franchise'`
    - `landing/routeMetadata.ts` contains `'/news'`
    - `landing/routeMetadata.ts` contains `首页`
    - `landing/routeMetadata.ts` contains `关于我们`
    - `landing/routeMetadata.ts` contains `产品服务`
    - `landing/routeMetadata.ts` contains `加盟合作`
    - `landing/routeMetadata.ts` contains `新闻动态`
  </acceptance_criteria>
  <done>The landing route namespace is defined in one file that later plans can reuse for layout and tests.</done>
</task>

<task type="auto">
  <name>Task 2: Create the dedicated landing app and router bootstrap</name>
  <files>landing/LandingApp.tsx, landing/routes.tsx</files>
  <read_first>.planning/phases/02-react-landing-foundation-post-reset/02-RESEARCH.md, .planning/phases/02-react-landing-foundation-post-reset/02-VALIDATION.md, landing/routeMetadata.ts, landing/LandingApp.tsx, landing/routes.tsx, package.json</read_first>
  <action>Create `landing/LandingApp.tsx` and `landing/routes.tsx` using `react-router-dom` declarative routing. `landing/LandingApp.tsx` must wrap the route tree in `BrowserRouter` with the exact `basename="/entry-station"` value and render a dedicated route component from `landing/routes.tsx`. `landing/routes.tsx` must define the five routes from `landing/routeMetadata.ts`, even if the rendered content is still placeholder-level in this plan. For now, each route can render a minimal placeholder node with a unique `data-testid` using the exact forms `landing-page-home`, `landing-page-about`, `landing-page-products`, `landing-page-franchise`, and `landing-page-news`. Also render a shared top-level root marker `data-testid="landing-router-root"` so later smoke tests have a stable shell assertion before the shared nav/footer work lands.</action>
  <automated>npm run build</automated>
  <verify>test -f landing/LandingApp.tsx && test -f landing/routes.tsx && rg -n 'basename=\"/entry-station\"|basename=\\\"/entry-station\\\"|basename=\"/entry-station\"' landing/LandingApp.tsx && rg -n "landing-router-root" landing/routes.tsx && rg -n "landing-page-home" landing/routes.tsx && rg -n "landing-page-about" landing/routes.tsx && rg -n "landing-page-products" landing/routes.tsx && rg -n "landing-page-franchise" landing/routes.tsx && rg -n "landing-page-news" landing/routes.tsx && npm run build</verify>
  <acceptance_criteria>
    - `landing/LandingApp.tsx` exists
    - `landing/LandingApp.tsx` contains `BrowserRouter`
    - `landing/LandingApp.tsx` contains `/entry-station`
    - `landing/routes.tsx` exists
    - `landing/routes.tsx` contains `landing-router-root`
    - `landing/routes.tsx` contains `landing-page-home`
    - `landing/routes.tsx` contains `landing-page-about`
    - `landing/routes.tsx` contains `landing-page-products`
    - `landing/routes.tsx` contains `landing-page-franchise`
    - `landing/routes.tsx` contains `landing-page-news`
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>The project has a dedicated landing router bootstrapped under the public `/entry-station` namespace.</done>
</task>

<task type="auto">
  <name>Task 3: Promote EntryShell to tri-mode path ownership</name>
  <files>EntryShell.tsx</files>
  <read_first>.planning/phases/02-react-landing-foundation-post-reset/02-CONTEXT.md, .planning/phases/02-react-landing-foundation-post-reset/02-RESEARCH.md, EntryShell.tsx, landing/LandingApp.tsx, public/entry-station/index.html</read_first>
  <action>Update `EntryShell.tsx` so path reading returns three explicit modes: `home` for `/`, `landing` for any pathname equal to `/entry-station` or starting with `/entry-station/`, and `academy` for `/academy`. Keep the iframe implementation for `home` unchanged, including the exact `src="/entry-station/index.html?v=20260311-3"` target. Keep the academy bridge logic unchanged for `window.__openAiAcademy` and `OPEN_AI_ACADEMY` postMessage handling. Add rendering logic so `landing` mode mounts `LandingApp` and `academy` mode still mounts `App`. Ensure browser back/forward still flows through the existing `popstate` listener by recalculating mode from pathname instead of storing a hardcoded two-state assumption.</action>
  <automated>npm run build</automated>
  <verify>rg -n "type EntryMode = 'home' \\| 'landing' \\| 'academy'" EntryShell.tsx && rg -n "/entry-station/index.html\\?v=20260311-3" EntryShell.tsx && rg -n "__openAiAcademy" EntryShell.tsx && rg -n "OPEN_AI_ACADEMY" EntryShell.tsx && rg -n "LandingApp" EntryShell.tsx && npm run build</verify>
  <acceptance_criteria>
    - `EntryShell.tsx` contains `type EntryMode = 'home' | 'landing' | 'academy'`
    - `EntryShell.tsx` contains `LandingApp`
    - `EntryShell.tsx` contains `/entry-station/index.html?v=20260311-3`
    - `EntryShell.tsx` contains `__openAiAcademy`
    - `EntryShell.tsx` contains `OPEN_AI_ACADEMY`
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>The host shell now hands `/entry-station*` to React while preserving `/` and `/academy` behavior.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `test -f landing/routeMetadata.ts && rg -n "首页|关于我们|产品服务|加盟合作|新闻动态" landing/routeMetadata.ts`
- [ ] `test -f landing/LandingApp.tsx && test -f landing/routes.tsx && rg -n "/entry-station" landing/LandingApp.tsx && rg -n "landing-router-root" landing/routes.tsx`
- [ ] `rg -n "type EntryMode = 'home' \| 'landing' \| 'academy'" EntryShell.tsx && rg -n "LandingApp" EntryShell.tsx && rg -n "/entry-station/index.html\\?v=20260311-3" EntryShell.tsx && rg -n "__openAiAcademy" EntryShell.tsx && rg -n "OPEN_AI_ACADEMY" EntryShell.tsx`
- [ ] `npm run build`
</verification>

<success_criteria>

- All tasks completed
- `/entry-station*` has explicit React shell ownership in the host shell
- `/` still keeps the iframe-based transitional runtime path intact
- `/academy` bridge behavior remains intact
- No errors or warnings introduced
  </success_criteria>

<output>
After completion, create `.planning/phases/02-react-landing-foundation-post-reset/02-02-SUMMARY.md`
</output>
