---
phase: 02-react-landing-foundation-post-reset
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - pnpm-lock.yaml
  - playwright.config.ts
  - tests/landing-routing.spec.ts
autonomous: true
requirements:
  - SHELL-02
user_setup: []
must_haves:
  truths:
    - Phase 2 uses a formal React route library rather than handwritten pathname branching for the landing route family.
    - Execution has a minimal browser-level smoke harness before route-ownership work begins, because `/entry-station` direct-load and refresh are browser behaviors rather than pure unit behaviors.
    - The smoke harness explicitly reserves test coverage for `entry-station shell ownership` and `five landing routes`, matching the validation strategy.
  artifacts:
    - package.json
    - package-lock.json
    - pnpm-lock.yaml
    - playwright.config.ts
    - tests/landing-routing.spec.ts
  key_links:
    - `package.json` includes `react-router-dom` and `@playwright/test` plus a reusable route-smoke command.
    - `playwright.config.ts` points preview traffic at `http://127.0.0.1:4173` so later plans can verify built `/entry-station` behavior rather than only dev behavior.
    - `tests/landing-routing.spec.ts` contains the exact test titles `entry-station shell ownership` and `five landing routes`.
---

<objective>
Establish the route-foundation dependencies and minimal browser smoke harness for Phase 2.

Purpose: Ensure the landing route work has an installed router and a deterministic verification path before shell arbitration and page scaffolding begin.
Output: Installed `react-router-dom` and `@playwright/test`, a committed Playwright config, and a named route-smoke spec skeleton ready for later route assertions.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-react-landing-foundation-post-reset/02-CONTEXT.md
@.planning/phases/02-react-landing-foundation-post-reset/02-RESEARCH.md
@.planning/phases/02-react-landing-foundation-post-reset/02-VALIDATION.md
@.planning/phases/01-baseline-lock-shell-contract/01-UI-SPEC.md
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install the routing and route-smoke dependencies</name>
  <files>package.json, package-lock.json, pnpm-lock.yaml</files>
  <read_first>.planning/REQUIREMENTS.md, .planning/phases/02-react-landing-foundation-post-reset/02-CONTEXT.md, .planning/phases/02-react-landing-foundation-post-reset/02-RESEARCH.md, .planning/phases/02-react-landing-foundation-post-reset/02-VALIDATION.md, package.json</read_first>
  <action>Update `package.json` so `dependencies` includes `react-router-dom` at `^7.14.1` and `devDependencies` includes `@playwright/test` at `^1.59.1`. Add a reusable script named `test:landing-routing` with the exact command `playwright test tests/landing-routing.spec.ts`. Run `npm install` so `package-lock.json` records both packages and the lockfile matches the new scripts and dependency graph. Because the repo also tracks `pnpm-lock.yaml`, immediately run `pnpm install --lockfile-only` so both lockfiles reflect the same dependency change instead of leaving the pnpm lock stale.</action>
  <automated>npm ls react-router-dom @playwright/test --depth=0 && node -e "const p=require('./package.json'); if(!p.scripts||p.scripts['test:landing-routing']!=='playwright test tests/landing-routing.spec.ts') process.exit(1)"</automated>
  <verify>npm ls react-router-dom @playwright/test --depth=0 && rg -n '"react-router-dom": "\\^7\\.14\\.1"' package.json && rg -n '"@playwright/test": "\\^1\\.59\\.1"' package.json && rg -n '"test:landing-routing": "playwright test tests/landing-routing\\.spec\\.ts"' package.json && rg -n "react-router-dom" pnpm-lock.yaml && rg -n "@playwright/test" pnpm-lock.yaml</verify>
  <acceptance_criteria>
    - `package.json` contains `"react-router-dom": "^7.14.1"`
    - `package.json` contains `"@playwright/test": "^1.59.1"`
    - `package.json` contains `"test:landing-routing": "playwright test tests/landing-routing.spec.ts"`
    - `npm ls react-router-dom @playwright/test --depth=0` exits 0
    - `package-lock.json` contains `react-router-dom`
    - `package-lock.json` contains `@playwright/test`
    - `pnpm-lock.yaml` contains `react-router-dom`
    - `pnpm-lock.yaml` contains `@playwright/test`
  </acceptance_criteria>
  <done>The workspace has the exact routing and smoke-test dependencies needed for the rest of the phase.</done>
</task>

<task type="auto">
  <name>Task 2: Create the preview-backed Playwright route smoke harness</name>
  <files>playwright.config.ts, tests/landing-routing.spec.ts</files>
  <read_first>.planning/phases/02-react-landing-foundation-post-reset/02-RESEARCH.md, .planning/phases/02-react-landing-foundation-post-reset/02-VALIDATION.md, package.json, playwright.config.ts, tests/landing-routing.spec.ts</read_first>
  <action>Create `playwright.config.ts` with a minimal configuration that uses `baseURL: 'http://127.0.0.1:4173'`, `testDir: './tests'`, and a `webServer` block whose command is the exact string `npm run preview -- --host 127.0.0.1 --port 4173`, with `url: 'http://127.0.0.1:4173'` and `reuseExistingServer: true`. Create `tests/landing-routing.spec.ts` with two named tests using the exact titles `entry-station shell ownership` and `five landing routes`. Mark both tests with `test.fixme` comments that explicitly say the assertions will be enabled after shell arbitration and page scaffold work in later Phase 2 plans. Ensure the file imports `test` and `expect` from `@playwright/test` and includes placeholder `page.goto(...)` lines for `/entry-station` and `/entry-station/about` so later plans extend the same spec instead of replacing it.</action>
  <automated>npx playwright test tests/landing-routing.spec.ts --list</automated>
  <verify>test -f playwright.config.ts && test -f tests/landing-routing.spec.ts && rg -n "http://127\\.0\\.0\\.1:4173" playwright.config.ts && rg -n "npm run preview -- --host 127\\.0\\.0\\.1 --port 4173" playwright.config.ts && rg -n "entry-station shell ownership" tests/landing-routing.spec.ts && rg -n "five landing routes" tests/landing-routing.spec.ts && rg -n "test\\.fixme" tests/landing-routing.spec.ts && npx playwright test tests/landing-routing.spec.ts --list</verify>
  <acceptance_criteria>
    - `playwright.config.ts` exists
    - `playwright.config.ts` contains `http://127.0.0.1:4173`
    - `playwright.config.ts` contains `npm run preview -- --host 127.0.0.1 --port 4173`
    - `tests/landing-routing.spec.ts` exists
    - `tests/landing-routing.spec.ts` contains `entry-station shell ownership`
    - `tests/landing-routing.spec.ts` contains `five landing routes`
    - `tests/landing-routing.spec.ts` contains `test.fixme`
    - `npx playwright test tests/landing-routing.spec.ts --list` exits 0
  </acceptance_criteria>
  <done>The project has a committed, runnable browser-smoke skeleton that later plans can activate instead of inventing verification from scratch.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm ls react-router-dom @playwright/test --depth=0`
- [ ] `rg -n '"test:landing-routing": "playwright test tests/landing-routing\.spec\.ts"' package.json`
- [ ] `rg -n "react-router-dom" pnpm-lock.yaml && rg -n "@playwright/test" pnpm-lock.yaml`
- [ ] `test -f playwright.config.ts && rg -n "http://127\.0\.0\.1:4173" playwright.config.ts && rg -n "npm run preview -- --host 127\.0\.0\.1 --port 4173" playwright.config.ts`
- [ ] `test -f tests/landing-routing.spec.ts && rg -n "entry-station shell ownership" tests/landing-routing.spec.ts && rg -n "five landing routes" tests/landing-routing.spec.ts && rg -n "test\.fixme" tests/landing-routing.spec.ts && npx playwright test tests/landing-routing.spec.ts --list`
</verification>

<success_criteria>

- All tasks completed
- The workspace has a formal routing dependency installed
- A minimal preview-backed route-smoke harness exists before shell arbitration work
- No errors or warnings introduced
  </success_criteria>

<output>
After completion, create `.planning/phases/02-react-landing-foundation-post-reset/02-01-SUMMARY.md`
</output>
