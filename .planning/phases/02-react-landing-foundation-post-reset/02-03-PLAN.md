---
phase: 02-react-landing-foundation-post-reset
plan: 03
type: execute
wave: 2
depends_on:
  - "02-02"
files_modified:
  - landing/components/LandingShell.tsx
  - landing/components/LandingNav.tsx
  - landing/components/LandingFooter.tsx
  - landing/pages/LandingHomePage.tsx
  - landing/pages/LandingAboutPage.tsx
  - landing/pages/LandingProductsPage.tsx
  - landing/pages/LandingFranchisePage.tsx
  - landing/pages/LandingNewsPage.tsx
  - landing/routes.tsx
  - tests/landing-routing.spec.ts
autonomous: false
requirements:
  - SHELL-02
user_setup: []
must_haves:
  truths:
    - Users must be able to navigate to five distinct React-owned landing destinations under `/entry-station`, with each destination rendering its own page identity.
    - A shared landing shell with nav frame, active state, and footer must appear across all five destinations.
    - Built preview verification must prove that `/entry-station/index.html` still serves the transitional static runtime while the route family itself is React-owned.
  artifacts:
    - landing/components/LandingShell.tsx
    - landing/components/LandingNav.tsx
    - landing/components/LandingFooter.tsx
    - landing/pages/LandingHomePage.tsx
    - landing/pages/LandingAboutPage.tsx
    - landing/pages/LandingProductsPage.tsx
    - landing/pages/LandingFranchisePage.tsx
    - landing/pages/LandingNewsPage.tsx
    - tests/landing-routing.spec.ts
  key_links:
    - `LandingShell.tsx` exposes the stable markers `landing-shell`, `landing-nav`, and `landing-footer`.
    - Each page file exposes one stable marker in the exact pattern `landing-page-{key}`.
    - `tests/landing-routing.spec.ts` activates both reserved smoke tests and asserts the five route family destinations plus the preserved static iframe target.
---

<objective>
Deliver the shared landing shell, five independent page scaffolds, and the final route smoke verification for Phase 2.

Purpose: Finish the user-visible route foundation so every scoped landing destination is reachable, identifiable, and provably under React ownership without cutting over `/entry-station/index.html`.
Output: Shared shell components, five scaffold pages, activated route-smoke tests, and preview/build verification for both React routes and the preserved static iframe target.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
@$HOME/.codex/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-react-landing-foundation-post-reset/02-CONTEXT.md
@.planning/phases/02-react-landing-foundation-post-reset/02-RESEARCH.md
@.planning/phases/02-react-landing-foundation-post-reset/02-VALIDATION.md
@.planning/phases/01-baseline-lock-shell-contract/01-UI-SPEC.md
@EntryShell.tsx
@landing/LandingApp.tsx
@landing/routes.tsx
@landing/routeMetadata.ts
@tests/landing-routing.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the shared landing shell and route-aware frame</name>
  <files>landing/components/LandingShell.tsx, landing/components/LandingNav.tsx, landing/components/LandingFooter.tsx, landing/routes.tsx</files>
  <read_first>.planning/phases/02-react-landing-foundation-post-reset/02-CONTEXT.md, .planning/phases/02-react-landing-foundation-post-reset/02-RESEARCH.md, .planning/phases/01-baseline-lock-shell-contract/01-UI-SPEC.md, landing/routeMetadata.ts, landing/routes.tsx, landing/components/LandingShell.tsx, landing/components/LandingNav.tsx, landing/components/LandingFooter.tsx</read_first>
  <action>Create `landing/components/LandingShell.tsx`, `landing/components/LandingNav.tsx`, and `landing/components/LandingFooter.tsx`. `LandingShell.tsx` must render stable wrappers with the exact `data-testid` values `landing-shell`, `landing-nav`, and `landing-footer`, and expose one content outlet region for child pages. `LandingNav.tsx` must render one item for each record in `landing/routeMetadata.ts` using router-aware navigation components so the current route gets an explicit active-state class or attribute. `LandingFooter.tsx` must be shared across every route and include at least one visible text label referencing `频安AI智能商学院`. Update `landing/routes.tsx` so all five destination pages render inside the shared shell rather than as standalone placeholder nodes.</action>
  <automated>npm run build</automated>
  <verify>test -f landing/components/LandingShell.tsx && test -f landing/components/LandingNav.tsx && test -f landing/components/LandingFooter.tsx && rg -n 'data-testid=\"landing-shell\"' landing/components/LandingShell.tsx && rg -n 'data-testid=\"landing-nav\"' landing/components/LandingShell.tsx landing/components/LandingNav.tsx && rg -n 'data-testid=\"landing-footer\"' landing/components/LandingShell.tsx landing/components/LandingFooter.tsx && rg -n '频安AI智能商学院' landing/components/LandingFooter.tsx && npm run build</verify>
  <acceptance_criteria>
    - `landing/components/LandingShell.tsx` exists
    - `landing/components/LandingNav.tsx` exists
    - `landing/components/LandingFooter.tsx` exists
    - `LandingShell.tsx` contains `data-testid="landing-shell"`
    - `LandingShell.tsx` or `LandingNav.tsx` contains `data-testid="landing-nav"`
    - `LandingShell.tsx` or `LandingFooter.tsx` contains `data-testid="landing-footer"`
    - `LandingFooter.tsx` contains `频安AI智能商学院`
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>The route family renders within one shared shell and exposes stable verification markers.</done>
</task>

<task type="auto">
  <name>Task 2: Create five independent scaffold pages and wire them to the shell</name>
  <files>landing/pages/LandingHomePage.tsx, landing/pages/LandingAboutPage.tsx, landing/pages/LandingProductsPage.tsx, landing/pages/LandingFranchisePage.tsx, landing/pages/LandingNewsPage.tsx, landing/routes.tsx</files>
  <read_first>.planning/phases/02-react-landing-foundation-post-reset/02-CONTEXT.md, .planning/phases/02-react-landing-foundation-post-reset/02-RESEARCH.md, landing/routeMetadata.ts, landing/routes.tsx, landing/pages/LandingHomePage.tsx, landing/pages/LandingAboutPage.tsx, landing/pages/LandingProductsPage.tsx, landing/pages/LandingFranchisePage.tsx, landing/pages/LandingNewsPage.tsx</read_first>
  <action>Create five page files named exactly `LandingHomePage.tsx`, `LandingAboutPage.tsx`, `LandingProductsPage.tsx`, `LandingFranchisePage.tsx`, and `LandingNewsPage.tsx`. Each page must export a component that renders its exact page heading text (`首页`, `关于我们`, `产品服务`, `加盟合作`, `新闻动态`) and a unique page marker using the exact `data-testid` values `landing-page-home`, `landing-page-about`, `landing-page-products`, `landing-page-franchise`, and `landing-page-news`. Each page must also render at least two scaffold placeholder sections with visible labels indicating future restoration scope, such as `页面骨架` and `内容待恢复`, so the route is more than a bare heading. Update `landing/routes.tsx` so every route record resolves to its corresponding page component.</action>
  <automated>npm run build</automated>
  <verify>test -f landing/pages/LandingHomePage.tsx && test -f landing/pages/LandingAboutPage.tsx && test -f landing/pages/LandingProductsPage.tsx && test -f landing/pages/LandingFranchisePage.tsx && test -f landing/pages/LandingNewsPage.tsx && rg -n 'data-testid=\"landing-page-home\"' landing/pages/LandingHomePage.tsx && rg -n 'data-testid=\"landing-page-about\"' landing/pages/LandingAboutPage.tsx && rg -n 'data-testid=\"landing-page-products\"' landing/pages/LandingProductsPage.tsx && rg -n 'data-testid=\"landing-page-franchise\"' landing/pages/LandingFranchisePage.tsx && rg -n 'data-testid=\"landing-page-news\"' landing/pages/LandingNewsPage.tsx && rg -n '页面骨架|内容待恢复' landing/pages/LandingHomePage.tsx landing/pages/LandingAboutPage.tsx landing/pages/LandingProductsPage.tsx landing/pages/LandingFranchisePage.tsx landing/pages/LandingNewsPage.tsx && npm run build</verify>
  <acceptance_criteria>
    - `landing/pages/LandingHomePage.tsx` exists and contains `data-testid="landing-page-home"`
    - `landing/pages/LandingAboutPage.tsx` exists and contains `data-testid="landing-page-about"`
    - `landing/pages/LandingProductsPage.tsx` exists and contains `data-testid="landing-page-products"`
    - `landing/pages/LandingFranchisePage.tsx` exists and contains `data-testid="landing-page-franchise"`
    - `landing/pages/LandingNewsPage.tsx` exists and contains `data-testid="landing-page-news"`
    - Every page file contains one of `页面骨架` or `内容待恢复`
    - `landing/routes.tsx` references all five page component names
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>The five landing destinations now exist as distinct scaffold pages ready for later restoration work.</done>
</task>

<task type="auto">
  <name>Task 3: Activate the route smoke tests and prove route/static coexistence</name>
  <files>tests/landing-routing.spec.ts</files>
  <read_first>.planning/phases/02-react-landing-foundation-post-reset/02-RESEARCH.md, .planning/phases/02-react-landing-foundation-post-reset/02-VALIDATION.md, tests/landing-routing.spec.ts, landing/components/LandingShell.tsx, landing/pages/LandingHomePage.tsx, landing/pages/LandingAboutPage.tsx, landing/pages/LandingProductsPage.tsx, landing/pages/LandingFranchisePage.tsx, landing/pages/LandingNewsPage.tsx, public/entry-station/index.html</read_first>
  <action>Replace the `test.fixme` placeholders in `tests/landing-routing.spec.ts` with active Playwright assertions. The test titled `entry-station shell ownership` must visit `/entry-station` and `/entry-station/about`, assert the presence of `data-testid="landing-shell"` and the correct page markers, and assert that navigation remains under the `/entry-station` path family. The test titled `five landing routes` must visit all five routes (`/entry-station`, `/entry-station/about`, `/entry-station/products`, `/entry-station/franchise`, `/entry-station/news`) and assert the shared shell plus each exact page marker. After the Playwright suite passes, run `npm run build` and `npm run preview -- --host 127.0.0.1 --port 4173` in a temporary preview session, then verify with `curl -sf http://127.0.0.1:4173/entry-station/index.html | rg '<!doctype html>'` that the preserved static iframe target remains reachable alongside the React route family.</action>
  <automated>npm run build && npx playwright test tests/landing-routing.spec.ts && PREVIEW_LOG=/tmp/phase02-route-preview.log && rm -f "$PREVIEW_LOG" && npm run preview -- --host 127.0.0.1 --port 4173 > "$PREVIEW_LOG" 2>&1 & PREVIEW_PID=$! && trap 'kill "$PREVIEW_PID" 2>/dev/null || true' EXIT && until curl -sf http://127.0.0.1:4173/entry-station/index.html >/dev/null; do sleep 1; done && curl -sf http://127.0.0.1:4173/entry-station/index.html | rg '<!doctype html>'</automated>
  <verify>rg -n "entry-station shell ownership" tests/landing-routing.spec.ts && rg -n "five landing routes" tests/landing-routing.spec.ts && ! rg -n "test\\.fixme" tests/landing-routing.spec.ts && npm run build && npx playwright test tests/landing-routing.spec.ts && PREVIEW_LOG=/tmp/phase02-route-preview.log && rm -f "$PREVIEW_LOG" && npm run preview -- --host 127.0.0.1 --port 4173 > "$PREVIEW_LOG" 2>&1 & PREVIEW_PID=$! && trap 'kill "$PREVIEW_PID" 2>/dev/null || true' EXIT && until curl -sf http://127.0.0.1:4173/entry-station/index.html >/dev/null; do sleep 1; done && curl -sf http://127.0.0.1:4173/entry-station/index.html | rg '<!doctype html>'</verify>
  <acceptance_criteria>
    - `tests/landing-routing.spec.ts` contains `entry-station shell ownership`
    - `tests/landing-routing.spec.ts` contains `five landing routes`
    - `tests/landing-routing.spec.ts` does not contain `test.fixme`
    - `npm run build` exits 0
    - `npx playwright test tests/landing-routing.spec.ts` exits 0
    - `curl -sf http://127.0.0.1:4173/entry-station/index.html | rg '<!doctype html>'` exits 0 during preview verification
  </acceptance_criteria>
  <done>The built app proves that React owns `/entry-station` route destinations while `/entry-station/index.html` remains the preserved static iframe target.</done>
</task>

<task type="checkpoint:human-verify" gate="non-blocking">
  <name>Task 4: Spot-check shared shell structure on desktop and mobile widths</name>
  <files>landing/components/LandingShell.tsx, landing/pages/LandingHomePage.tsx, landing/pages/LandingNewsPage.tsx</files>
  <action>After the automated route smoke passes, ask for a quick human visual check of `/entry-station`, `/entry-station/about`, and `/entry-station/news` on one desktop viewport and one narrow mobile viewport. The check is limited to structure only: shared nav frame persists, footer persists, and page identity remains obvious. If no human is available, record that the manual check remains pending and continue because this is non-blocking for Phase 2 planning quality.</action>
  <what-built>Shared landing shell, five page scaffolds, and automated route smoke coverage for the route family.</what-built>
  <how-to-verify>Open `/entry-station`, `/entry-station/about`, and `/entry-station/news` in desktop and narrow mobile widths. Confirm the shared shell remains present and each destination still has a distinct page identity marker.</how-to-verify>
  <resume-signal>Type "approved" or note manual follow-up needed</resume-signal>
  <automated>npm run build && npx playwright test tests/landing-routing.spec.ts</automated>
  <verify>Human approved the structural shell check, or the execution summary explicitly records the manual follow-up as pending.</verify>
  <done>The plan captures the last remaining manual-only structural verification without blocking execution if a reviewer is unavailable.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `test -f landing/components/LandingShell.tsx && test -f landing/components/LandingNav.tsx && test -f landing/components/LandingFooter.tsx && rg -n 'data-testid="landing-shell"' landing/components/LandingShell.tsx && rg -n 'data-testid="landing-nav"' landing/components/LandingShell.tsx landing/components/LandingNav.tsx && rg -n 'data-testid="landing-footer"' landing/components/LandingShell.tsx landing/components/LandingFooter.tsx`
- [ ] `test -f landing/pages/LandingHomePage.tsx && test -f landing/pages/LandingAboutPage.tsx && test -f landing/pages/LandingProductsPage.tsx && test -f landing/pages/LandingFranchisePage.tsx && test -f landing/pages/LandingNewsPage.tsx && rg -n 'landing-page-home|landing-page-about|landing-page-products|landing-page-franchise|landing-page-news' landing/pages/*.tsx`
- [ ] `npm run build && npx playwright test tests/landing-routing.spec.ts`
- [ ] `PREVIEW_LOG=/tmp/phase02-route-preview.log && rm -f "$PREVIEW_LOG" && npm run preview -- --host 127.0.0.1 --port 4173 > "$PREVIEW_LOG" 2>&1 & PREVIEW_PID=$! && trap 'kill "$PREVIEW_PID" 2>/dev/null || true' EXIT && until curl -sf http://127.0.0.1:4173/entry-station/index.html >/dev/null; do sleep 1; done && curl -sf http://127.0.0.1:4173/entry-station/index.html | rg '<!doctype html>'`
</verification>

<success_criteria>

- All tasks completed
- Five distinct React-owned landing destinations exist under `/entry-station`
- Shared landing shell and navigation frame render across all five destinations
- Built preview proves route-family ownership while preserving the static iframe target
- No errors or warnings introduced
  </success_criteria>

<output>
After completion, create `.planning/phases/02-react-landing-foundation-post-reset/02-03-SUMMARY.md`
</output>
