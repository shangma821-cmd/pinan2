---
phase: 01-baseline-lock-shell-contract
plan: 03
type: execute
wave: 3
depends_on:
  - "01-02"
files_modified:
  - .planning/phases/01-baseline-lock-shell-contract/SHELL-CONTRACT-CHECKLIST.md
  - dist/entry-station/index.html
autonomous: false
requirements:
  - SHELL-01
  - SHELL-03
user_setup: []
must_haves:
  truths:
    - A fresh build still emits `dist/entry-station/index.html` for the locked `/entry-station` public path.
    - The agent starts a local preview environment before the human checkpoint so verification stays visual-only.
    - The checklist records approved manual smoke results for `/`, landing -> academy, and academy -> `/`.
  artifacts:
    - .planning/phases/01-baseline-lock-shell-contract/SHELL-CONTRACT-CHECKLIST.md
    - dist/entry-station/index.html
  key_links:
    - `SHELL-CONTRACT-CHECKLIST.md` records `Build command: PASS (npm run build)`, `Build artifact: PASS (dist/entry-station/index.html)`, `Preview URL: http://127.0.0.1:4273`, and `Preview PID:` before human review begins.
    - `SHELL-CONTRACT-CHECKLIST.md` records `Manual pass: /: PASS (approved)`, `Manual pass: landing -> academy: PASS (approved)`, and `Manual pass: academy -> /: PASS (approved)` after the checkpoint returns `approved`.
---

<objective>
Run the build gate, start a local preview, and collect the final shell smoke approval for Phase 1.

Purpose: Convert the written shell contract into reviewable build and human-smoke evidence without changing shell behavior or landing content.
Output: Fresh build output at `dist/entry-station/index.html` plus a checklist that contains both automated build evidence and approved manual smoke results.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
@$HOME/.codex/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-baseline-lock-shell-contract/01-CONTEXT.md
@.planning/phases/01-baseline-lock-shell-contract/01-VALIDATION.md
@.planning/phases/01-baseline-lock-shell-contract/SHELL-CONTRACT-CHECKLIST.md
@docs/superpowers/specs/2026-04-09-entry-station-routing-design.md
@package.json
@EntryShell.tsx
@vite.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the app, start preview, and record automated shell evidence</name>
  <files>.planning/phases/01-baseline-lock-shell-contract/SHELL-CONTRACT-CHECKLIST.md, dist/entry-station/index.html</files>
  <read_first>.planning/REQUIREMENTS.md, docs/superpowers/specs/2026-04-09-entry-station-routing-design.md, .planning/phases/01-baseline-lock-shell-contract/01-VALIDATION.md, .planning/phases/01-baseline-lock-shell-contract/SHELL-CONTRACT-CHECKLIST.md, package.json, EntryShell.tsx, vite.config.ts</read_first>
  <action>Run `npm run build`. Confirm `dist/entry-station/index.html` exists. Before starting preview, delete any stale `/tmp/phase01-shell-preview.pid` and `/tmp/phase01-shell-preview.log`, then assert `lsof -ti tcp:4273` returns nothing; if the port is already occupied, fail the task instead of reusing it. Start preview in the background with `npm run preview -- --host 127.0.0.1 --port 4273 > /tmp/phase01-shell-preview.log 2>&1 &`, wait until `curl -sfI http://127.0.0.1:4273/entry-station/index.html` succeeds, then capture the actual listener PID with `PREVIEW_PID=$(lsof -ti tcp:4273 | head -n1)` and write that value to `/tmp/phase01-shell-preview.pid`. Verify both `kill -0 "$PREVIEW_PID"` and `lsof -ti tcp:4273 | grep -qx "$PREVIEW_PID"` succeed so the PID file records the real preview listener that will be reviewed and later stopped. Update the `Verification Record` section in `.planning/phases/01-baseline-lock-shell-contract/SHELL-CONTRACT-CHECKLIST.md` so it contains these exact lines: `Build command: PASS (npm run build)`, `Build artifact: PASS (dist/entry-station/index.html)`, `Preview URL: http://127.0.0.1:4273`, and `Preview PID: $PREVIEW_PID`. Do not edit `EntryShell.tsx` or `vite.config.ts` in this plan.</action>
  <automated>npm run build && test -f dist/entry-station/index.html && [ -f /tmp/phase01-shell-preview.pid ] && PREVIEW_PID=$(cat /tmp/phase01-shell-preview.pid) && [ -n "$PREVIEW_PID" ] && kill -0 "$PREVIEW_PID" && lsof -ti tcp:4273 | grep -qx "$PREVIEW_PID" && curl -sfI http://127.0.0.1:4273/entry-station/index.html >/dev/null && rg -n "Build command: PASS \\(npm run build\\)" .planning/phases/01-baseline-lock-shell-contract/SHELL-CONTRACT-CHECKLIST.md && rg -n "Build artifact: PASS \\(dist/entry-station/index.html\\)" .planning/phases/01-baseline-lock-shell-contract/SHELL-CONTRACT-CHECKLIST.md && rg -n "Preview URL: http://127.0.0.1:4273" .planning/phases/01-baseline-lock-shell-contract/SHELL-CONTRACT-CHECKLIST.md && rg -n "Preview PID: $PREVIEW_PID" .planning/phases/01-baseline-lock-shell-contract/SHELL-CONTRACT-CHECKLIST.md</automated>
  <verify>npm run build && test -f dist/entry-station/index.html && [ -f /tmp/phase01-shell-preview.pid ] && PREVIEW_PID=$(cat /tmp/phase01-shell-preview.pid) && [ -n "$PREVIEW_PID" ] && kill -0 "$PREVIEW_PID" && lsof -ti tcp:4273 | grep -qx "$PREVIEW_PID" && curl -sfI http://127.0.0.1:4273/entry-station/index.html >/dev/null && rg -n "Build command: PASS \\(npm run build\\)" .planning/phases/01-baseline-lock-shell-contract/SHELL-CONTRACT-CHECKLIST.md && rg -n "Build artifact: PASS \\(dist/entry-station/index.html\\)" .planning/phases/01-baseline-lock-shell-contract/SHELL-CONTRACT-CHECKLIST.md && rg -n "Preview URL: http://127.0.0.1:4273" .planning/phases/01-baseline-lock-shell-contract/SHELL-CONTRACT-CHECKLIST.md && rg -n "Preview PID: $PREVIEW_PID" .planning/phases/01-baseline-lock-shell-contract/SHELL-CONTRACT-CHECKLIST.md</verify>
  <acceptance_criteria>
    - `npm run build` exits 0
    - `dist/entry-station/index.html` exists after the build
    - `/tmp/phase01-shell-preview.pid` exists after preview starts
    - `cat /tmp/phase01-shell-preview.pid` returns a non-empty listener PID
    - `kill -0 "$(cat /tmp/phase01-shell-preview.pid)"` exits 0
    - `lsof -ti tcp:4273 | grep -qx "$(cat /tmp/phase01-shell-preview.pid)"` exits 0
    - `curl -sfI http://127.0.0.1:4273/entry-station/index.html` exits 0
    - `SHELL-CONTRACT-CHECKLIST.md` contains `Build command: PASS (npm run build)`
    - `SHELL-CONTRACT-CHECKLIST.md` contains `Build artifact: PASS (dist/entry-station/index.html)`
    - `SHELL-CONTRACT-CHECKLIST.md` contains `Preview URL: http://127.0.0.1:4273`
    - `SHELL-CONTRACT-CHECKLIST.md` contains `Preview PID:`
  </acceptance_criteria>
  <done>The build artifact exists and the preview environment is ready for human verification.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Collect manual shell smoke approval</name>
  <files>.planning/phases/01-baseline-lock-shell-contract/SHELL-CONTRACT-CHECKLIST.md, dist/entry-station/index.html</files>
  <action>Pause after the agent has built the app and started preview on `http://127.0.0.1:4273`. Ask the human to verify the three locked shell behaviors and continue only when they reply `approved`. Use the PID and port guard from Task 1 as the automated prerequisite for this checkpoint.</action>
  <what-built>Fresh build plus preview server at `http://127.0.0.1:4273`, with `dist/entry-station/index.html` already generated and recorded in the checklist.</what-built>
  <how-to-verify>Visit `http://127.0.0.1:4273/` and verify only these visual/functional outcomes: `/` still shows the landing through the existing shell iframe, using the landing academy entry reaches `/academy`, and using the academy return action goes back to `/`. Reply `approved` if all three pass, or describe the failing step.</how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
  <automated>test -f dist/entry-station/index.html && [ -f /tmp/phase01-shell-preview.pid ] && PREVIEW_PID=$(cat /tmp/phase01-shell-preview.pid) && [ -n "$PREVIEW_PID" ] && kill -0 "$PREVIEW_PID" && lsof -ti tcp:4273 | grep -qx "$PREVIEW_PID" && curl -sfI http://127.0.0.1:4273/entry-station/index.html >/dev/null && rg -n "Build command: PASS \\(npm run build\\)" .planning/phases/01-baseline-lock-shell-contract/SHELL-CONTRACT-CHECKLIST.md && rg -n "Preview URL: http://127.0.0.1:4273" .planning/phases/01-baseline-lock-shell-contract/SHELL-CONTRACT-CHECKLIST.md && rg -n "Preview PID: $PREVIEW_PID" .planning/phases/01-baseline-lock-shell-contract/SHELL-CONTRACT-CHECKLIST.md</automated>
  <verify>Human replied `approved` after checking `/`, landing -> academy, and academy -> `/`</verify>
  <done>The checkpoint has either approved the shell smoke flow or blocked the plan with specific issues.</done>
</task>

<task type="auto">
  <name>Task 3: Persist manual approval in the checklist and stop preview</name>
  <files>.planning/phases/01-baseline-lock-shell-contract/SHELL-CONTRACT-CHECKLIST.md</files>
  <read_first>.planning/phases/01-baseline-lock-shell-contract/SHELL-CONTRACT-CHECKLIST.md, .planning/phases/01-baseline-lock-shell-contract/01-VALIDATION.md, EntryShell.tsx, vite.config.ts</read_first>
  <action>After the checkpoint returns `approved`, update the `Verification Record` section in `.planning/phases/01-baseline-lock-shell-contract/SHELL-CONTRACT-CHECKLIST.md` to include these exact lines: `Manual pass: /: PASS (approved)`, `Manual pass: landing -> academy: PASS (approved)`, and `Manual pass: academy -> /: PASS (approved)`. Then stop the preview server using `kill "$(cat /tmp/phase01-shell-preview.pid)"` if that PID file exists, delete `/tmp/phase01-shell-preview.pid`, and confirm `lsof -ti tcp:4273` returns nothing.</action>
  <automated>rg -n "Manual pass: /: PASS \\(approved\\)" .planning/phases/01-baseline-lock-shell-contract/SHELL-CONTRACT-CHECKLIST.md && rg -n "Manual pass: landing -> academy: PASS \\(approved\\)" .planning/phases/01-baseline-lock-shell-contract/SHELL-CONTRACT-CHECKLIST.md && rg -n "Manual pass: academy -> /: PASS \\(approved\\)" .planning/phases/01-baseline-lock-shell-contract/SHELL-CONTRACT-CHECKLIST.md && [ ! -f /tmp/phase01-shell-preview.pid ] && ! lsof -ti tcp:4273 >/dev/null</automated>
  <verify>rg -n "Manual pass: /: PASS \\(approved\\)" .planning/phases/01-baseline-lock-shell-contract/SHELL-CONTRACT-CHECKLIST.md && rg -n "Manual pass: landing -> academy: PASS \\(approved\\)" .planning/phases/01-baseline-lock-shell-contract/SHELL-CONTRACT-CHECKLIST.md && rg -n "Manual pass: academy -> /: PASS \\(approved\\)" .planning/phases/01-baseline-lock-shell-contract/SHELL-CONTRACT-CHECKLIST.md && [ ! -f /tmp/phase01-shell-preview.pid ] && ! lsof -ti tcp:4273 >/dev/null</verify>
  <acceptance_criteria>
    - `SHELL-CONTRACT-CHECKLIST.md` contains `Manual pass: /: PASS (approved)`
    - `SHELL-CONTRACT-CHECKLIST.md` contains `Manual pass: landing -> academy: PASS (approved)`
    - `SHELL-CONTRACT-CHECKLIST.md` contains `Manual pass: academy -> /: PASS (approved)`
    - `/tmp/phase01-shell-preview.pid` does not exist
    - `lsof -ti tcp:4273` returns no listener
  </acceptance_criteria>
  <done>The phase has reviewer-readable manual smoke evidence and no leftover preview PID file.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build && test -f dist/entry-station/index.html`
- [ ] `[ -f /tmp/phase01-shell-preview.pid ] && PREVIEW_PID=$(cat /tmp/phase01-shell-preview.pid) && [ -n "$PREVIEW_PID" ] && kill -0 "$PREVIEW_PID" && lsof -ti tcp:4273 | grep -qx "$PREVIEW_PID" && curl -sfI http://127.0.0.1:4273/entry-station/index.html >/dev/null`
- [ ] `PREVIEW_PID=$(cat /tmp/phase01-shell-preview.pid) && rg -n "Build command: PASS \\(npm run build\\)" .planning/phases/01-baseline-lock-shell-contract/SHELL-CONTRACT-CHECKLIST.md && rg -n "Build artifact: PASS \\(dist/entry-station/index.html\\)" .planning/phases/01-baseline-lock-shell-contract/SHELL-CONTRACT-CHECKLIST.md && rg -n "Preview URL: http://127.0.0.1:4273" .planning/phases/01-baseline-lock-shell-contract/SHELL-CONTRACT-CHECKLIST.md && rg -n "Preview PID: $PREVIEW_PID" .planning/phases/01-baseline-lock-shell-contract/SHELL-CONTRACT-CHECKLIST.md`
- [ ] `rg -n "Manual pass: /: PASS \\(approved\\)" .planning/phases/01-baseline-lock-shell-contract/SHELL-CONTRACT-CHECKLIST.md && rg -n "Manual pass: landing -> academy: PASS \\(approved\\)" .planning/phases/01-baseline-lock-shell-contract/SHELL-CONTRACT-CHECKLIST.md && rg -n "Manual pass: academy -> /: PASS \\(approved\\)" .planning/phases/01-baseline-lock-shell-contract/SHELL-CONTRACT-CHECKLIST.md`
</verification>

<success_criteria>

- All tasks completed
- Build output still emits `dist/entry-station/index.html`
- Preview environment is ready before the human checkpoint runs
- Checklist records approved manual smoke results for `/`, landing -> academy, and academy -> `/`
- No errors or warnings introduced
  </success_criteria>

<output>
After completion, create `.planning/phases/01-baseline-lock-shell-contract/01-03-SUMMARY.md`
</output>
