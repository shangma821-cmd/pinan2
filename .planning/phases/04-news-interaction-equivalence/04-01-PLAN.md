---
phase: 04-news-interaction-equivalence
plan: 01
type: execute
wave: 2
depends_on:
  - "04-02"
files_modified:
  - landing/assets.ts
  - landing/content/newsContent.ts
  - landing/pages/LandingNewsPage.tsx
  - landing/sections/home/HomeNewsPreview.tsx
  - landing/landing.css
autonomous: true
requirements:
  - NEWS-01
  - NEWS-02
  - NEWS-03
must_haves:
  truths:
    - `/entry-station/news` remains the only News route, and detail state is driven exclusively by the `?id=` query param.
    - News content stays project-local and static in this phase; no API calls, CMS hooks, or remote fetch logic are introduced.
    - The Home news preview and the dedicated News page read from the same News content source so preview/detail parity cannot drift.
  artifacts:
    - landing/assets.ts
    - landing/content/newsContent.ts
    - landing/pages/LandingNewsPage.tsx
    - landing/sections/home/HomeNewsPreview.tsx
    - landing/landing.css
  key_links:
    - `landing/assets.ts` exports the extra news image keys `newsCellRepair`, `newsCertification`, and `newsOpening`.
    - `landing/content/newsContent.ts` exports exactly six articles plus the category list `全部`, `行业动态`, `科研成果`, `公司新闻`.
    - `landing/pages/LandingNewsPage.tsx` uses `useSearchParams` to read `id`, filter the list, and clear detail state back to `/entry-station/news`.
    - `landing/sections/home/HomeNewsPreview.tsx` imports the shared News content module instead of hardcoding a second article list.
---

<objective>
Restore the full News browsing experience with shared local content, client-side search/filter behavior, and same-route detail handling.

Purpose: Satisfy `NEWS-01`, `NEWS-02`, and `NEWS-03` with a baseline-faithful News page while keeping the established `/news?id=` contract.
Output: A shared News content module, a substantive `LandingNewsPage`, aligned Home preview cards, and landing-scoped News styles.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-news-interaction-equivalence/04-CONTEXT.md
@.planning/phases/04-news-interaction-equivalence/04-RESEARCH.md
@.planning/phases/04-news-interaction-equivalence/04-VALIDATION.md
@.planning/phases/01-baseline-lock-shell-contract/01-UI-SPEC.md
@landing/assets.ts
@landing/pages/LandingNewsPage.tsx
@landing/sections/home/HomeNewsPreview.tsx
@landing/landing.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create one shared local News content module and complete the News asset registry</name>
  <files>landing/assets.ts, landing/content/newsContent.ts, landing/sections/home/HomeNewsPreview.tsx</files>
  <read_first>.planning/phases/04-news-interaction-equivalence/04-CONTEXT.md, .planning/phases/04-news-interaction-equivalence/04-RESEARCH.md, .planning/phases/01-baseline-lock-shell-contract/01-UI-SPEC.md, landing/assets.ts, landing/sections/home/HomeNewsPreview.tsx, .planning/baselines/kimi-landing-7be7097-parent/Kimi_Agent_Deployment_v14/assets/index-BqLXAaHJ.js</read_first>
  <action>Extend `landing/assets.ts` with the exact new keys `newsCellRepair:'/entry-station/news-cell-repair.jpg'`, `newsCertification:'/entry-station/news-certification.jpg'`, and `newsOpening:'/entry-station/news-opening.jpg'`. Create `landing/content/newsContent.ts` exporting `landingNewsCategories` as the exact array `['全部', '行业动态', '科研成果', '公司新闻']` and `landingNewsArticles` as exactly 6 records with the keys `id`, `title`, `excerpt`, `image`, `date`, `category`, `author`, `readTime`, and `content`. Use these exact titles and categories: `1 频谱治疗的未来发展趋势 / 行业动态`, `2 细胞修复技术突破性研究 / 科研成果`, `3 频安健康荣获创新企业奖 / 公司新闻`, `4 国家黄十字亚健康服务体系升级 / 行业动态`, `5 智能健康设备市场持续增长 / 行业动态`, `6 频安健康深圳首家旗舰店开业 / 公司新闻`. Map article images to `newsAiHealth`, `newsCellRepair`, `newsAward`, `newsCertification`, `newsMarket`, and `newsOpening` respectively. Update `landing/sections/home/HomeNewsPreview.tsx` so it imports `landingNewsArticles`, uses the first 3 records as preview cards, and keeps preview links in the exact form ``/news?id=${article.id}``.</action>
  <automated>test -f landing/content/newsContent.ts && rg -n "newsCellRepair|newsCertification|newsOpening" landing/assets.ts && rg -n "landingNewsCategories|landingNewsArticles" landing/content/newsContent.ts && rg -n "频谱治疗的未来发展趋势|细胞修复技术突破性研究|频安健康荣获创新企业奖|国家黄十字亚健康服务体系升级|智能健康设备市场持续增长|频安健康深圳首家旗舰店开业" landing/content/newsContent.ts && rg -n "全部|行业动态|科研成果|公司新闻" landing/content/newsContent.ts && rg -n "landingNewsArticles" landing/sections/home/HomeNewsPreview.tsx && npm run build</automated>
  <verify>test -f landing/content/newsContent.ts && rg -n "/entry-station/news-cell-repair.jpg|/entry-station/news-certification.jpg|/entry-station/news-opening.jpg" landing/assets.ts && rg -n "id:\\s*1|id:\\s*6" landing/content/newsContent.ts && rg -n "category:\\s*'科研成果'|category:\\s*'公司新闻'" landing/content/newsContent.ts && rg -n "to=\\{`/news\\?id=\\$\\{article\\.id\\}`\\}" landing/sections/home/HomeNewsPreview.tsx && npm run build</verify>
  <acceptance_criteria>
    - `landing/assets.ts` contains `newsCellRepair`
    - `landing/assets.ts` contains `newsCertification`
    - `landing/assets.ts` contains `newsOpening`
    - `landing/content/newsContent.ts` exists
    - `landing/content/newsContent.ts` contains `landingNewsCategories`
    - `landing/content/newsContent.ts` contains `landingNewsArticles`
    - `landing/content/newsContent.ts` contains `频谱治疗的未来发展趋势`
    - `landing/content/newsContent.ts` contains `细胞修复技术突破性研究`
    - `landing/content/newsContent.ts` contains `频安健康深圳首家旗舰店开业`
    - `landing/content/newsContent.ts` contains `全部`
    - `landing/content/newsContent.ts` contains `科研成果`
    - `landing/sections/home/HomeNewsPreview.tsx` contains `landingNewsArticles`
    - `landing/sections/home/HomeNewsPreview.tsx` contains ``/news?id=${article.id}``
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>The landing runtime has one authoritative local News dataset that both preview and full-page flows can share.</done>
</task>

<task type="auto">
  <name>Task 2: Replace the News placeholder page with list, search, filter, and detail states</name>
  <files>landing/pages/LandingNewsPage.tsx, landing/landing.css</files>
  <read_first>.planning/phases/04-news-interaction-equivalence/04-CONTEXT.md, .planning/phases/04-news-interaction-equivalence/04-RESEARCH.md, .planning/phases/01-baseline-lock-shell-contract/01-UI-SPEC.md, landing/pages/LandingNewsPage.tsx, landing/content/newsContent.ts, landing/landing.css, tests/landing-routing.spec.ts</read_first>
  <action>Rewrite `landing/pages/LandingNewsPage.tsx` so it imports `useSearchParams` from `react-router-dom` and the shared `landingNewsArticles` / `landingNewsCategories` exports. Implement list state with `const [selectedCategory, setSelectedCategory] = useState('全部')` and `const [query, setQuery] = useState('')`. Compute filtered results by matching `title` or `excerpt` against `query.toLowerCase()` and by keeping `selectedCategory === '全部' || article.category === selectedCategory`. When `searchParams.get('id')` resolves to an existing article id, render a detail layout that visibly includes the article `title`, `category`, `date`, `author`, `readTime`, full `content`, and a button with the exact text `返回列表` that clears the query string back to the list state. When no detail id is active, render the list header copy `资讯动态`, a search input with placeholder `搜索资讯...`, category pills for the exact 4 categories, article cards linking to ``/news?id=${article.id}``, and an empty state with the exact text `没有找到相关资讯`. Add deterministic hooks: `data-testid=\"landing-page-news\"`, `data-testid=\"news-search-input\"`, `data-testid=\"news-filter-全部\"`, `data-testid=\"news-filter-科研成果\"`, `data-testid=\"news-detail-view\"`, and `data-testid=\"news-return-button\"`. Extend `landing/landing.css` with namespaced classes for News list grid, category pills, detail hero/meta rows, and empty state without removing existing landing classes.</action>
  <automated>rg -n "useSearchParams|news-search-input|news-filter-全部|news-detail-view|news-return-button|搜索资讯\\.\\.\\.|返回列表|没有找到相关资讯" landing/pages/LandingNewsPage.tsx && rg -n "landing-news-grid|landing-news-filter|landing-news-detail|landing-news-empty" landing/landing.css && npm run build</automated>
  <verify>rg -n "selectedCategory|setSelectedCategory|query|setQuery" landing/pages/LandingNewsPage.tsx && rg -n "searchParams\\.get\\('id'\\)|searchParams\\.get\\(\"id\"\\)" landing/pages/LandingNewsPage.tsx && rg -n "landingNewsCategories\\.map|landingNewsArticles\\.find|landingNewsArticles\\.filter" landing/pages/LandingNewsPage.tsx && rg -n "data-testid=\\\"landing-page-news\\\"|data-testid=\\\"news-detail-view\\\"|data-testid=\\\"news-return-button\\\"" landing/pages/LandingNewsPage.tsx && npm run build</verify>
  <acceptance_criteria>
    - `landing/pages/LandingNewsPage.tsx` contains `useSearchParams`
    - `landing/pages/LandingNewsPage.tsx` contains `搜索资讯...`
    - `landing/pages/LandingNewsPage.tsx` contains `返回列表`
    - `landing/pages/LandingNewsPage.tsx` contains `没有找到相关资讯`
    - `landing/pages/LandingNewsPage.tsx` contains `data-testid="news-search-input"`
    - `landing/pages/LandingNewsPage.tsx` contains `data-testid="news-filter-科研成果"`
    - `landing/pages/LandingNewsPage.tsx` contains `data-testid="news-detail-view"`
    - `landing/pages/LandingNewsPage.tsx` contains `data-testid="news-return-button"`
    - `landing/pages/LandingNewsPage.tsx` contains ``/news?id=${article.id}``
    - `landing/landing.css` contains `landing-news-grid`
    - `landing/landing.css` contains `landing-news-detail`
    - `landing/landing.css` contains `landing-news-empty`
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>The News route now supports full list/search/filter/detail behavior without changing the canonical route contract.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `test -f landing/content/newsContent.ts && rg -n "landingNewsCategories|landingNewsArticles" landing/content/newsContent.ts`
- [ ] `rg -n "newsCellRepair|newsCertification|newsOpening" landing/assets.ts`
- [ ] `rg -n "useSearchParams|搜索资讯\\.\\.\\.|返回列表|没有找到相关资讯" landing/pages/LandingNewsPage.tsx`
- [ ] `rg -n "data-testid=\"news-search-input\"|data-testid=\"news-detail-view\"|data-testid=\"news-return-button\"" landing/pages/LandingNewsPage.tsx`
- [ ] `rg -n "landing-news-grid|landing-news-detail|landing-news-empty" landing/landing.css`
- [ ] `npm run build`
</verification>

<success_criteria>

- All tasks completed
- `/entry-station/news` provides both list and detail states
- Search, category filtering, and empty-state behavior are implemented client-side
- Home preview and News detail read from one local content source
  </success_criteria>

<output>
After completion, create `.planning/phases/04-news-interaction-equivalence/04-01-SUMMARY.md`
</output>
