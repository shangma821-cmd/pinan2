---
phase: 04-news-interaction-equivalence
plan: 04
type: execute
wave: 4
depends_on:
  - "04-01"
  - "04-02"
  - "04-03"
files_modified:
  - tests/landing-routing.spec.ts
  - tests/landing-core-pages.spec.ts
  - tests/landing-news-interactions.spec.ts
autonomous: false
requirements:
  - HOME-03
  - NEWS-01
  - NEWS-02
  - NEWS-03
  - XPG-01
  - XPG-02
must_haves:
  truths:
    - Browser verification exists for every Phase 4 requirement ID, and route smoke still protects the five-route landing family.
    - News behaviors are tested through the real browser flow: list, search, filter, query-string detail, and return-to-list.
    - Shared shell interaction behaviors are tested through the real browser flow: theme persistence, visible day/night theme affordances, mobile overlay navigation, scroll-reactive nav hooks, and back-to-top.
  artifacts:
    - tests/landing-routing.spec.ts
    - tests/landing-core-pages.spec.ts
    - tests/landing-news-interactions.spec.ts
  key_links:
    - `tests/landing-news-interactions.spec.ts` contains the exact test titles `news list search and category filters`, `news detail opens from query string and returns to list`, `theme persists across landing navigation and reload`, and `mobile navigation overlay and back-to-top work across landing routes`.
    - `tests/landing-news-interactions.spec.ts` asserts `data-scrolled="true"` on `landing-nav` after scrolling.
    - `tests/landing-core-pages.spec.ts` contains the exact new title `home interaction parity`.
    - Full verification uses `npm run build && npx playwright test tests/landing-routing.spec.ts tests/landing-core-pages.spec.ts tests/landing-news-interactions.spec.ts`.
---

<objective>
Harden Phase 4 verification so execution can prove News and cross-page parity instead of relying on visual claims.

Purpose: Turn Phase 4 into a browser-tested delivery pack that covers every requirement ID and leaves only narrow manual visual checks.
Output: A new News/interactions Playwright spec, updated route/core-page coverage, and a final visual checkpoint note.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
@$HOME/.codex/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-news-interaction-equivalence/04-CONTEXT.md
@.planning/phases/04-news-interaction-equivalence/04-RESEARCH.md
@.planning/phases/04-news-interaction-equivalence/04-VALIDATION.md
@playwright.config.ts
@tests/landing-routing.spec.ts
@tests/landing-core-pages.spec.ts
@landing/pages/LandingNewsPage.tsx
@landing/components/LandingNav.tsx
@landing/components/LandingFooter.tsx
@landing/sections/home/HomeResults.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a dedicated Phase 4 Playwright spec for News and shared-shell interactions</name>
  <files>tests/landing-news-interactions.spec.ts</files>
  <read_first>.planning/phases/04-news-interaction-equivalence/04-RESEARCH.md, .planning/phases/04-news-interaction-equivalence/04-VALIDATION.md, playwright.config.ts, landing/pages/LandingNewsPage.tsx, landing/components/LandingNav.tsx, landing/components/LandingFooter.tsx, landing/sections/home/HomeNewsPreview.tsx</read_first>
  <action>Create `tests/landing-news-interactions.spec.ts` with these exact test titles: `news list search and category filters`, `news detail opens from query string and returns to list`, `theme persists across landing navigation and reload`, and `mobile navigation overlay and back-to-top work across landing routes`. In `news list search and category filters`, go to `/entry-station/news`, assert `data-testid=\"news-search-input\"`, click the filter `data-testid=\"news-filter-科研成果\"`, assert `细胞修复技术突破性研究` is visible, then fill the search input with `不存在的关键词` and assert `没有找到相关资讯`. In `news detail opens from query string and returns to list`, open `/entry-station/news?id=2`, assert `data-testid=\"news-detail-view\"`, assert the title `细胞修复技术突破性研究`, click `data-testid=\"news-return-button\"`, and assert the URL ends with `/entry-station/news`. In `theme persists across landing navigation and reload`, assert the theme toggle visibly contains either `日间模式` or `夜间模式` and either `☀` or `☾`, click `data-testid=\"landing-theme-toggle\"`, assert the visible copy/icon pair changes, navigate between `/entry-station` and `/entry-station/news`, reload, and assert `document.documentElement.getAttribute('data-theme')` and `localStorage.getItem('theme')` are equal and not empty. In `mobile navigation overlay and back-to-top work across landing routes`, set a narrow viewport, open `data-testid=\"landing-mobile-menu-toggle\"`, assert `data-testid=\"landing-mobile-menu\"`, navigate to `新闻动态`, scroll down, assert `page.getByTestId('landing-nav')` has `data-scrolled=\"true\"`, click `data-testid=\"landing-back-to-top\"`, and assert the scroll position returns near the top.</action>
  <automated>test -f tests/landing-news-interactions.spec.ts && rg -n "news list search and category filters|news detail opens from query string and returns to list|theme persists across landing navigation and reload|mobile navigation overlay and back-to-top work across landing routes" tests/landing-news-interactions.spec.ts && npm run build && npx playwright test tests/landing-news-interactions.spec.ts</automated>
  <verify>rg -n "news-search-input|news-filter-科研成果|news-detail-view|news-return-button|landing-theme-toggle|landing-mobile-menu-toggle|landing-mobile-menu|landing-back-to-top|data-scrolled" tests/landing-news-interactions.spec.ts && rg -n "细胞修复技术突破性研究|没有找到相关资讯|日间模式|夜间模式|☀|☾" tests/landing-news-interactions.spec.ts && npm run build && npx playwright test tests/landing-news-interactions.spec.ts</verify>
  <acceptance_criteria>
    - `tests/landing-news-interactions.spec.ts` exists
    - `tests/landing-news-interactions.spec.ts` contains `news list search and category filters`
    - `tests/landing-news-interactions.spec.ts` contains `news detail opens from query string and returns to list`
    - `tests/landing-news-interactions.spec.ts` contains `theme persists across landing navigation and reload`
    - `tests/landing-news-interactions.spec.ts` contains `mobile navigation overlay and back-to-top work across landing routes`
    - `tests/landing-news-interactions.spec.ts` contains `news-search-input`
    - `tests/landing-news-interactions.spec.ts` contains `news-return-button`
    - `tests/landing-news-interactions.spec.ts` contains `landing-theme-toggle`
    - `tests/landing-news-interactions.spec.ts` contains `landing-back-to-top`
    - `tests/landing-news-interactions.spec.ts` contains `data-scrolled`
    - `tests/landing-news-interactions.spec.ts` contains `日间模式`
    - `tests/landing-news-interactions.spec.ts` contains `夜间模式`
    - `tests/landing-news-interactions.spec.ts` contains `☀`
    - `tests/landing-news-interactions.spec.ts` contains `☾`
    - `npx playwright test tests/landing-news-interactions.spec.ts` exits 0
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Phase 4 now has a dedicated browser spec for News and cross-page interaction requirements.</done>
</task>

<task type="auto">
  <name>Task 2: Update existing route and Home specs to absorb Phase 4 parity hooks</name>
  <files>tests/landing-routing.spec.ts, tests/landing-core-pages.spec.ts</files>
  <read_first>.planning/phases/04-news-interaction-equivalence/04-RESEARCH.md, .planning/phases/04-news-interaction-equivalence/04-VALIDATION.md, tests/landing-routing.spec.ts, tests/landing-core-pages.spec.ts, landing/components/LandingNav.tsx, landing/sections/home/HomeAdvantages.tsx, landing/sections/home/HomeProcess.tsx, landing/sections/home/HomeResults.tsx</read_first>
  <action>Update `tests/landing-routing.spec.ts` while keeping the exact existing titles `entry-station shell ownership` and `five landing routes`. Add assertions that the shared shell exposes `data-testid=\"landing-theme-toggle\"` and `data-testid=\"landing-back-to-top\"` on React-owned landing routes. Update `tests/landing-core-pages.spec.ts` to add one exact new test title `home interaction parity`. That test must open `/entry-station`, click the advantages trigger containing `模式壁垒：公平返利，用户自动裂变`, assert the corresponding `data-active=\"true\"` state or expanded detail is present, click the process step `数据追踪`, assert the active step hook flips to true, and confirm the results section eventually shows `5000+` and `92%` through the results test IDs.</action>
  <automated>rg -n "entry-station shell ownership|five landing routes" tests/landing-routing.spec.ts && rg -n "landing-theme-toggle|landing-back-to-top" tests/landing-routing.spec.ts && rg -n "home interaction parity" tests/landing-core-pages.spec.ts && npm run build && npx playwright test tests/landing-routing.spec.ts tests/landing-core-pages.spec.ts</automated>
  <verify>rg -n "模式壁垒：公平返利，用户自动裂变|数据追踪|5000\\+|92%" tests/landing-core-pages.spec.ts && rg -n "landing-theme-toggle|landing-back-to-top" tests/landing-routing.spec.ts && npm run build && npx playwright test tests/landing-routing.spec.ts tests/landing-core-pages.spec.ts</verify>
  <acceptance_criteria>
    - `tests/landing-routing.spec.ts` contains `entry-station shell ownership`
    - `tests/landing-routing.spec.ts` contains `five landing routes`
    - `tests/landing-routing.spec.ts` contains `landing-theme-toggle`
    - `tests/landing-routing.spec.ts` contains `landing-back-to-top`
    - `tests/landing-core-pages.spec.ts` contains `home interaction parity`
    - `tests/landing-core-pages.spec.ts` contains `模式壁垒：公平返利，用户自动裂变`
    - `tests/landing-core-pages.spec.ts` contains `数据追踪`
    - `tests/landing-core-pages.spec.ts` contains `5000+`
    - `tests/landing-core-pages.spec.ts` contains `92%`
    - `npx playwright test tests/landing-routing.spec.ts tests/landing-core-pages.spec.ts` exits 0
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>The pre-existing browser suite now covers the upgraded shell hooks and the last Home interaction requirement.</done>
</task>

<task type="checkpoint:human-verify" gate="non-blocking">
  <name>Task 3: Spot-check desktop and mobile interaction feel after the browser suite passes</name>
  <files>landing/components/LandingNav.tsx, landing/components/LandingFooter.tsx, landing/pages/LandingNewsPage.tsx, .planning/phases/04-news-interaction-equivalence/04-04-SUMMARY.md</files>
  <read_first>.planning/phases/04-news-interaction-equivalence/04-RESEARCH.md, .planning/phases/04-news-interaction-equivalence/04-VALIDATION.md, landing/components/LandingNav.tsx, landing/components/LandingFooter.tsx, landing/pages/LandingNewsPage.tsx</read_first>
  <action>After the automated Phase 4 suite passes, do one desktop and one narrow-mobile manual pass on `/entry-station` and `/entry-station/news`. Confirm the transparent-to-scrolled nav transition still feels intentional, the mobile overlay reads as a floating menu rather than a generic drawer, the theme toggle remains legible in both modes, and the News detail hero/meta layout remains readable. If a human reviewer is unavailable, record the visual follow-up explicitly and continue because this checkpoint is non-blocking for plan completeness. When writing `.planning/phases/04-news-interaction-equivalence/04-04-SUMMARY.md`, include one exact line beginning with `Manual interaction spot-check:` followed by either `approved` or `pending visual follow-up`.</action>
  <what-built>Automated News, shell, and Home interaction verification across the full Phase 4 surface.</what-built>
  <how-to-verify>Open Home and News list/detail on desktop and narrow mobile widths, toggle theme, open the mobile menu, and verify the interaction surfaces still look baseline-led after the automation suite passes.</how-to-verify>
  <resume-signal>Type "approved" or note the remaining visual follow-up</resume-signal>
  <automated>npm run build && npx playwright test tests/landing-routing.spec.ts tests/landing-core-pages.spec.ts tests/landing-news-interactions.spec.ts</automated>
  <verify>Human approved the spot-check, or the summary explicitly records the remaining visual follow-up.</verify>
  <acceptance_criteria>
    - `.planning/phases/04-news-interaction-equivalence/04-04-SUMMARY.md` contains `Manual interaction spot-check:`
    - `.planning/phases/04-news-interaction-equivalence/04-04-SUMMARY.md` contains either `approved` or `pending visual follow-up`
    - `npm run build && npx playwright test tests/landing-routing.spec.ts tests/landing-core-pages.spec.ts tests/landing-news-interactions.spec.ts` exits 0 before the summary is finalized
  </acceptance_criteria>
  <done>Phase 4 verification includes both deterministic browser proof and a narrow manual interaction-feel check.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `test -f tests/landing-news-interactions.spec.ts && rg -n "news list search and category filters|news detail opens from query string and returns to list|theme persists across landing navigation and reload|mobile navigation overlay and back-to-top work across landing routes" tests/landing-news-interactions.spec.ts`
- [ ] `rg -n "home interaction parity" tests/landing-core-pages.spec.ts`
- [ ] `rg -n "landing-theme-toggle|landing-back-to-top" tests/landing-routing.spec.ts`
- [ ] `rg -n "data-scrolled|日间模式|夜间模式|☀|☾" tests/landing-news-interactions.spec.ts`
- [ ] `npm run build`
- [ ] `npx playwright test tests/landing-routing.spec.ts tests/landing-core-pages.spec.ts tests/landing-news-interactions.spec.ts`
</verification>

<success_criteria>

- All tasks completed
- Browser tests exist for every Phase 4 requirement ID
- Route smoke still guards the five landing destinations
- Only narrow manual visual checks remain after automation passes
  </success_criteria>

<output>
After completion, create `.planning/phases/04-news-interaction-equivalence/04-04-SUMMARY.md`
</output>
