---
phase: 04-news-interaction-equivalence
plan: 03
type: execute
wave: 3
depends_on:
  - "04-02"
  - "04-01"
files_modified:
  - landing/sections/home/HomeAdvantages.tsx
  - landing/sections/home/HomeProcess.tsx
  - landing/sections/home/HomeResults.tsx
  - landing/landing.css
autonomous: true
requirements:
  - HOME-03
must_haves:
  truths:
    - Phase 3 section order remains unchanged; this plan only restores missing interaction fidelity for `HOME-03`.
    - Home counters animate into their final values `5000+`, `92%`, `45%`, and `60%+` without replacing the existing content labels.
    - Existing advantages/process behavior stays deterministic and gains testable active-state hooks instead of being rewritten into a different interaction model.
  artifacts:
    - landing/sections/home/HomeAdvantages.tsx
    - landing/sections/home/HomeProcess.tsx
    - landing/sections/home/HomeResults.tsx
    - landing/landing.css
  key_links:
    - `landing/sections/home/HomeResults.tsx` uses `IntersectionObserver` to start the counter animation only after the section enters view.
    - `landing/sections/home/HomeAdvantages.tsx` keeps `aria-expanded` and adds stable active-state hooks for the currently opened item.
    - `landing/sections/home/HomeProcess.tsx` keeps the 5000ms auto-advance and exposes deterministic active-state hooks for the selected step.
---

<objective>
Close the remaining Home interaction gap without reworking the accepted Phase 3 structure.

Purpose: Satisfy `HOME-03` by restoring result-counter presentation parity and adding stable, testable state hooks to the already-restored advantages/process interactions.
Output: Animated results counters, deterministic Home interaction hooks, and any required landing-scoped styling for those behaviors.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-news-interaction-equivalence/04-CONTEXT.md
@.planning/phases/04-news-interaction-equivalence/04-RESEARCH.md
@.planning/phases/04-news-interaction-equivalence/04-VALIDATION.md
@landing/sections/home/HomeAdvantages.tsx
@landing/sections/home/HomeProcess.tsx
@landing/sections/home/HomeResults.tsx
@landing/landing.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Animate the Home results counters from in-view trigger to the exact final values</name>
  <files>landing/sections/home/HomeResults.tsx, landing/landing.css</files>
  <read_first>.planning/phases/04-news-interaction-equivalence/04-CONTEXT.md, .planning/phases/04-news-interaction-equivalence/04-RESEARCH.md, landing/sections/home/HomeResults.tsx, landing/landing.css, .planning/baselines/kimi-landing-7be7097-parent/Kimi_Agent_Deployment_v14/assets/index-BqLXAaHJ.js</read_first>
  <action>Update `landing/sections/home/HomeResults.tsx` so the current static `proofStats` strings are replaced by structured numeric values that can count up to the exact visible outputs `5000+`, `92%`, `45%`, and `60%+`. Add a section ref plus `IntersectionObserver` that starts the animation once when the results section enters view, and preserve the existing labels `体验用户沉淀`, `服务满意度`, `转介绍转化率提升`, and `单店年增长空间`. Add deterministic test hooks `data-testid=\"home-results-stat-users\"`, `data-testid=\"home-results-stat-satisfaction\"`, `data-testid=\"home-results-stat-referral\"`, and `data-testid=\"home-results-stat-growth\"`. Extend `landing/landing.css` only as needed for any counter-state styling or motion-safe fallback classes.</action>
  <automated>rg -n "IntersectionObserver|home-results-stat-users|home-results-stat-satisfaction|5000\\+|92%|45%|60%\\+" landing/sections/home/HomeResults.tsx && npm run build</automated>
  <verify>rg -n "data-testid=\"home-results-stat-users\"|data-testid=\"home-results-stat-growth\"" landing/sections/home/HomeResults.tsx && rg -n "体验用户沉淀|服务满意度|转介绍转化率提升|单店年增长空间" landing/sections/home/HomeResults.tsx && npm run build</verify>
  <acceptance_criteria>
    - `landing/sections/home/HomeResults.tsx` contains `IntersectionObserver`
    - `landing/sections/home/HomeResults.tsx` contains `data-testid="home-results-stat-users"`
    - `landing/sections/home/HomeResults.tsx` contains `data-testid="home-results-stat-growth"`
    - `landing/sections/home/HomeResults.tsx` contains `5000+`
    - `landing/sections/home/HomeResults.tsx` contains `92%`
    - `landing/sections/home/HomeResults.tsx` contains `45%`
    - `landing/sections/home/HomeResults.tsx` contains `60%+`
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>The Home results section now presents baseline-style numeric proof instead of static placeholders.</done>
</task>

<task type="auto">
  <name>Task 2: Add stable active-state hooks to the existing advantages and process interactions</name>
  <files>landing/sections/home/HomeAdvantages.tsx, landing/sections/home/HomeProcess.tsx</files>
  <read_first>.planning/phases/04-news-interaction-equivalence/04-CONTEXT.md, .planning/phases/04-news-interaction-equivalence/04-RESEARCH.md, landing/sections/home/HomeAdvantages.tsx, landing/sections/home/HomeProcess.tsx, tests/landing-core-pages.spec.ts</read_first>
  <action>Keep the current interaction model in `landing/sections/home/HomeAdvantages.tsx` and `landing/sections/home/HomeProcess.tsx`, but add deterministic hooks for browser verification. In `HomeAdvantages.tsx`, add `data-testid=\"home-advantages-item-{index}\"` to each article wrapper and `data-active={isActive ? 'true' : 'false'}` to the active container or trigger while preserving `aria-expanded`. In `HomeProcess.tsx`, keep the 5000ms auto-advance interval exactly as-is, add `data-testid=\"home-process-step-{index}\"` to each step button, and add `data-active={isActive ? 'true' : 'false'}` so tests can assert the selected state after a click. Do not change the visible copy or step order.</action>
  <automated>rg -n "data-testid=\\\"home-advantages-item-\\{|data-active=\\{isActive \\? 'true' : 'false'\\}" landing/sections/home/HomeAdvantages.tsx && rg -n "5000\\)|data-testid=\\\"home-process-step-\\{|data-active=\\{isActive \\? 'true' : 'false'\\}" landing/sections/home/HomeProcess.tsx && npm run build</automated>
  <verify>rg -n "aria-expanded" landing/sections/home/HomeAdvantages.tsx && rg -n "setInterval\\(|5000" landing/sections/home/HomeProcess.tsx && rg -n "data-testid=\\\"home-process-step-" landing/sections/home/HomeProcess.tsx && npm run build</verify>
  <acceptance_criteria>
    - `landing/sections/home/HomeAdvantages.tsx` contains `aria-expanded`
    - `landing/sections/home/HomeAdvantages.tsx` contains `data-testid="home-advantages-item-`
    - `landing/sections/home/HomeAdvantages.tsx` contains `data-active={isActive ? 'true' : 'false'}`
    - `landing/sections/home/HomeProcess.tsx` contains `setInterval`
    - `landing/sections/home/HomeProcess.tsx` contains `5000`
    - `landing/sections/home/HomeProcess.tsx` contains `data-testid="home-process-step-`
    - `landing/sections/home/HomeProcess.tsx` contains `data-active={isActive ? 'true' : 'false'}`
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>The Home interactions remain functionally the same but become verifiable through stable DOM state hooks.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rg -n "IntersectionObserver|home-results-stat-users|5000\\+|92%|45%|60%\\+" landing/sections/home/HomeResults.tsx`
- [ ] `rg -n "data-testid=\"home-advantages-item-|data-active=\\{isActive \\? 'true' : 'false'\\}" landing/sections/home/HomeAdvantages.tsx`
- [ ] `rg -n "setInterval\\(|5000|data-testid=\"home-process-step-" landing/sections/home/HomeProcess.tsx`
- [ ] `npm run build`
</verification>

<success_criteria>

- All tasks completed
- Home results animate to the exact baseline-facing values
- Advantages and process interactions stay intact and expose stable active-state hooks
- No page-structure changes are introduced to the accepted Home composition
  </success_criteria>

<output>
After completion, create `.planning/phases/04-news-interaction-equivalence/04-03-SUMMARY.md`
</output>
