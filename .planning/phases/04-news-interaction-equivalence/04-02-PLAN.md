---
phase: 04-news-interaction-equivalence
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - landing/LandingApp.tsx
  - landing/contexts/LandingThemeContext.tsx
  - landing/components/LandingShell.tsx
  - landing/components/LandingNav.tsx
  - landing/components/LandingFooter.tsx
  - landing/landing.css
autonomous: true
requirements:
  - XPG-01
  - XPG-02
must_haves:
  truths:
    - Theme persistence uses the exact contract `localStorage("theme")` plus `document.documentElement[data-theme]`, with `light` as the default mode.
    - The theme control preserves visible day/night copy and a sun/moon affordance rather than collapsing into an unlabeled icon-only toggle.
    - Shared cross-page interactions stay inside the existing React landing shell; no second nav system or non-landing global state is introduced.
    - Desktop nav becomes scroll-reactive after the page scroll offset passes 50px, mobile nav opens as an overlay/menu state, and footer exposes a smooth scroll-to-top affordance.
  artifacts:
    - landing/LandingApp.tsx
    - landing/contexts/LandingThemeContext.tsx
    - landing/components/LandingShell.tsx
    - landing/components/LandingNav.tsx
    - landing/components/LandingFooter.tsx
    - landing/landing.css
  key_links:
    - `landing/LandingApp.tsx` wraps the router with `LandingThemeProvider`.
    - `landing/contexts/LandingThemeContext.tsx` reads and writes the exact `theme` localStorage key and sets the root `data-theme` attribute.
    - `landing/components/LandingNav.tsx` uses `window.scrollY > 50`, exposes `data-scrolled`, and renders visible `日间模式` / `夜间模式` copy plus `☀` / `☾` affordances for the theme control.
    - `landing/components/LandingFooter.tsx` calls `window.scrollTo({ top: 0, behavior: 'smooth' })` from a visible back-to-top control.
---

<objective>
Restore shared landing-shell interactions: theme persistence, scroll-reactive nav behavior, mobile overlay navigation, and back-to-top support.

Purpose: Satisfy `XPG-01` and `XPG-02` by making the existing shell behave like the approved baseline across all five landing routes.
Output: A landing theme context/provider, upgraded shell/nav/footer interaction logic, and theme-aware shell styling.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-news-interaction-equivalence/04-CONTEXT.md
@.planning/phases/04-news-interaction-equivalence/04-RESEARCH.md
@.planning/phases/04-news-interaction-equivalence/04-VALIDATION.md
@.planning/phases/01-baseline-lock-shell-contract/01-UI-SPEC.md
@landing/LandingApp.tsx
@landing/components/LandingShell.tsx
@landing/components/LandingNav.tsx
@landing/components/LandingFooter.tsx
@landing/landing.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a landing-only theme provider that persists the exact baseline theme contract</name>
  <files>landing/LandingApp.tsx, landing/contexts/LandingThemeContext.tsx</files>
  <read_first>.planning/phases/04-news-interaction-equivalence/04-CONTEXT.md, .planning/phases/04-news-interaction-equivalence/04-RESEARCH.md, .planning/phases/01-baseline-lock-shell-contract/01-UI-SPEC.md, landing/LandingApp.tsx, .planning/baselines/kimi-landing-7be7097-parent/Kimi_Agent_Deployment_v14/assets/index-BqLXAaHJ.js</read_first>
  <action>Create `landing/contexts/LandingThemeContext.tsx` exporting `LandingThemeProvider` and `useLandingTheme`. The provider must initialize state from `localStorage.getItem('theme')`, default to `'light'` when the key is absent, call `document.documentElement.setAttribute('data-theme', theme)` whenever the theme changes, and persist the current mode through `localStorage.setItem('theme', theme)`. Expose a `toggleTheme()` helper that flips `'light'` and `'dark'`. Update `landing/LandingApp.tsx` so `<BrowserRouter basename=\"/entry-station\">` is wrapped inside `<LandingThemeProvider>...</LandingThemeProvider>` and no academy-level files are modified.</action>
  <automated>test -f landing/contexts/LandingThemeContext.tsx && rg -n "localStorage\\.getItem\\('theme'\\)|localStorage\\.setItem\\('theme'\\)|setAttribute\\('data-theme'" landing/contexts/LandingThemeContext.tsx && rg -n "LandingThemeProvider" landing/LandingApp.tsx landing/contexts/LandingThemeContext.tsx && npm run build</automated>
  <verify>test -f landing/contexts/LandingThemeContext.tsx && rg -n "toggleTheme|useLandingTheme|theme === 'dark' \\? 'light' : 'dark'" landing/contexts/LandingThemeContext.tsx && rg -n "BrowserRouter basename=\"/entry-station\"" landing/LandingApp.tsx && rg -n "<LandingThemeProvider>|</LandingThemeProvider>" landing/LandingApp.tsx && npm run build</verify>
  <acceptance_criteria>
    - `landing/contexts/LandingThemeContext.tsx` exists
    - `landing/contexts/LandingThemeContext.tsx` contains `localStorage.getItem('theme')`
    - `landing/contexts/LandingThemeContext.tsx` contains `localStorage.setItem('theme'`
    - `landing/contexts/LandingThemeContext.tsx` contains `setAttribute('data-theme'`
    - `landing/contexts/LandingThemeContext.tsx` contains `toggleTheme`
    - `landing/LandingApp.tsx` contains `LandingThemeProvider`
    - `landing/LandingApp.tsx` still contains `BrowserRouter basename="/entry-station"`
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>The landing runtime owns its own baseline-compatible theme state without leaking into academy runtime files.</done>
</task>

<task type="auto">
  <name>Task 2: Upgrade nav and footer interactions for scroll, mobile overlay, theme toggle, and back-to-top</name>
  <files>landing/components/LandingShell.tsx, landing/components/LandingNav.tsx, landing/components/LandingFooter.tsx, landing/landing.css</files>
  <read_first>.planning/phases/04-news-interaction-equivalence/04-CONTEXT.md, .planning/phases/04-news-interaction-equivalence/04-RESEARCH.md, .planning/phases/01-baseline-lock-shell-contract/01-UI-SPEC.md, landing/components/LandingShell.tsx, landing/components/LandingNav.tsx, landing/components/LandingFooter.tsx, landing/contexts/LandingThemeContext.tsx, landing/routeMetadata.ts, landing/landing.css</read_first>
  <action>Update `landing/components/LandingNav.tsx` to import `useLandingTheme`, track a `isMenuOpen` boolean, and track nav scroll state with a listener that sets a scrolled flag when `window.scrollY > 50`. The nav root must preserve `data-testid=\"landing-nav\"` and add `data-scrolled={isScrolled ? 'true' : 'false'}` plus a `landing-nav--scrolled` class when the threshold is crossed. Add a theme toggle button with `data-testid=\"landing-theme-toggle\"` and `aria-label=\"切换主题\"` that visibly renders `日间模式` together with `☀` in light mode and `夜间模式` together with `☾` in dark mode. Add a mobile menu trigger with `data-testid=\"landing-mobile-menu-toggle\"` and `aria-label=\"打开导航菜单\"`. Render the route list both in desktop nav and in a mobile overlay panel with `data-testid=\"landing-mobile-menu\"`, and close the overlay when a route link is clicked. Update `landing/components/LandingFooter.tsx` to render a visible button with `data-testid=\"landing-back-to-top\"`, `aria-label=\"返回顶部\"`, and a click handler that calls `window.scrollTo({ top: 0, behavior: 'smooth' })`. Update `landing/components/LandingShell.tsx` only as needed to preserve `landing-shell`, `landing-nav`, and `landing-footer` markers while supporting the new shell state. Extend `landing/landing.css` with theme-aware root styles for `[data-theme='dark']`, a `landing-nav--scrolled` modifier class, mobile overlay/menu classes, and a back-to-top control class. Keep the five-route nav metadata source unchanged.</action>
  <automated>rg -n "window\\.scrollY > 50|data-scrolled=|landing-nav--scrolled|landing-theme-toggle|landing-mobile-menu-toggle|landing-mobile-menu|日间模式|夜间模式|☀|☾|切换主题|打开导航菜单" landing/components/LandingNav.tsx && rg -n "window\\.scrollTo\\(\\{ top: 0, behavior: 'smooth' \\}\\)|landing-back-to-top|返回顶部" landing/components/LandingFooter.tsx && rg -n "data-theme='dark'|landing-nav--scrolled|landing-mobile-overlay|landing-mobile-menu|landing-back-to-top" landing/landing.css && npm run build</automated>
  <verify>rg -n "data-testid=\"landing-nav\"|data-testid=\"landing-footer\"" landing/components/LandingShell.tsx landing/components/LandingNav.tsx landing/components/LandingFooter.tsx && rg -n "useLandingTheme" landing/components/LandingNav.tsx && rg -n "setIsMenuOpen\\(|isMenuOpen" landing/components/LandingNav.tsx && rg -n "data-testid=\"landing-theme-toggle\"|data-testid=\"landing-mobile-menu-toggle\"|data-testid=\"landing-mobile-menu\"" landing/components/LandingNav.tsx && rg -n "日间模式|夜间模式|☀|☾" landing/components/LandingNav.tsx && rg -n "data-testid=\"landing-back-to-top\"" landing/components/LandingFooter.tsx && npm run build</verify>
  <acceptance_criteria>
    - `landing/components/LandingNav.tsx` contains `window.scrollY > 50`
    - `landing/components/LandingNav.tsx` contains `data-scrolled={isScrolled ? 'true' : 'false'}`
    - `landing/components/LandingNav.tsx` contains `data-testid="landing-theme-toggle"`
    - `landing/components/LandingNav.tsx` contains `data-testid="landing-mobile-menu-toggle"`
    - `landing/components/LandingNav.tsx` contains `data-testid="landing-mobile-menu"`
    - `landing/components/LandingNav.tsx` contains `aria-label="切换主题"`
    - `landing/components/LandingNav.tsx` contains `aria-label="打开导航菜单"`
    - `landing/components/LandingNav.tsx` contains `日间模式`
    - `landing/components/LandingNav.tsx` contains `夜间模式`
    - `landing/components/LandingNav.tsx` contains `☀`
    - `landing/components/LandingNav.tsx` contains `☾`
    - `landing/components/LandingFooter.tsx` contains `data-testid="landing-back-to-top"`
    - `landing/components/LandingFooter.tsx` contains `window.scrollTo({ top: 0, behavior: 'smooth' })`
    - `landing/landing.css` contains `[data-theme='dark']`
    - `landing/landing.css` contains `landing-nav--scrolled`
    - `landing/landing.css` contains `landing-mobile-menu`
    - `landing/landing.css` contains `landing-back-to-top`
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>The shared shell now exposes baseline-style persistent theme, mobile navigation, scroll-reactive nav, and back-to-top behavior across landing pages.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `test -f landing/contexts/LandingThemeContext.tsx && rg -n "localStorage.getItem\\('theme'\\)|localStorage.setItem\\('theme'\\)|setAttribute\\('data-theme'" landing/contexts/LandingThemeContext.tsx`
- [ ] `rg -n "data-testid=\"landing-theme-toggle\"|data-testid=\"landing-mobile-menu-toggle\"|data-testid=\"landing-mobile-menu\"" landing/components/LandingNav.tsx`
- [ ] `rg -n "window.scrollY > 50|data-scrolled=|日间模式|夜间模式|☀|☾" landing/components/LandingNav.tsx`
- [ ] `rg -n "data-testid=\"landing-back-to-top\"|window.scrollTo\\(\\{ top: 0, behavior: 'smooth' \\}\\)" landing/components/LandingFooter.tsx`
- [ ] `rg -n "\\[data-theme='dark'\\]|landing-nav--scrolled|landing-mobile-menu|landing-back-to-top" landing/landing.css`
- [ ] `npm run build`
</verification>

<success_criteria>

- All tasks completed
- Landing theme persists via `localStorage("theme")`
- Nav reacts to scroll, exposes mobile overlay navigation, and preserves route metadata ownership
- Footer exposes a working back-to-top control
  </success_criteria>

<output>
After completion, create `.planning/phases/04-news-interaction-equivalence/04-02-SUMMARY.md`
</output>
